    free_resolve_attempt += stats.free_resolve_attempt;
    free_resolve_shared_probe += stats.free_resolve_shared_probe;
    free_resolve_shared_valid += stats.free_resolve_shared_valid;
    free_resolve_shared_miss += stats.free_resolve_shared_miss;
    free_resolve_shared_retry += stats.free_resolve_shared_retry;
    free_resolve_shared_stale += stats.free_resolve_shared_stale;
    free_resolve_local_lookup += stats.free_resolve_local_lookup;
    free_resolve_visible_shadow += stats.free_resolve_visible_shadow;
    free_resolve_visible_hit += stats.free_resolve_visible_hit;
    free_resolve_result_local_valid +=
        stats.free_resolve_result_local_valid;
    free_resolve_result_foreign_valid +=
        stats.free_resolve_result_foreign_valid;
    free_resolve_result_owned_invalid +=
        stats.free_resolve_result_owned_invalid;
    free_resolve_result_proven_external +=
        stats.free_resolve_result_proven_external;
    free_resolve_result_retry += stats.free_resolve_result_retry;
    free_resolve_result_unresolved_integrity +=
        stats.free_resolve_result_unresolved_integrity;
