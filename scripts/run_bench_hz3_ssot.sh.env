# Defaults for hz3 SSOT runs (override-friendly)
: "${RUNS:=10}"
: "${ITERS:=20000000}"
: "${WS:=400}"
: "${SKIP_BUILD:=0}"
: "${HZ3_CLEAN:=1}"
# Optional extra args passed to BENCH_BIN (space-separated).
# Examples:
#   BENCH_BIN=./hakozuna/out/bench_random_mixed_malloc_dist BENCH_EXTRA_ARGS="app"
#   BENCH_BIN=./hakozuna/out/bench_random_mixed_malloc_dist BENCH_EXTRA_ARGS="trimix 16 256 80 257 2048 15 2049 32768 5"
#   BENCH_EXTRA_ARGS="0x12345678 app"  (seed + dist)
# Optional preload libs
# MIMALLOC_SO=/usr/lib/x86_64-linux-gnu/libmimalloc.so
# TCMALLOC_SO=/usr/lib/x86_64-linux-gnu/libtcmalloc.so
