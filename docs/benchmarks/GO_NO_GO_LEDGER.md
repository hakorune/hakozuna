# GO / NO-GO Ledger

This ledger is the quick status view for the current benchmark and lane families.
It is intentionally compact. Use the linked work orders for details, and use this
page to answer "what is live, what is frozen, and what was already rejected?"

Legend:
- `GO/default`: promoted into the live lane
- `GO(tooling)`: build or lane tooling that is now safe to use
- `FROZEN`: currently live, but the optimization line is exhausted
- `NO-GO`: tested and rejected for the current goal
- `ARCHIVED`: kept for history only

## Ledger

| Area | Status | Evidence | Note |
|---|---|---|---|
| HZ8 native Ubuntu `Page8K-TargetDispatch-L1` | `GO(research)/HOLD(default)` | GCC/Clang smoke, safety, and page8K API passed. Native alternating AB/BA R10 measured v2/target: fixed8K `197.614M/260.648M` (+31.90%), balanced `264.537M/254.694M` (-3.72%), wide `242.478M/256.246M` (+5.68%), larger `110.566M/98.721M` (-10.71%), with equal median RSS on all four rows | Keep as an explicit child lane with old R3 rollback. It clears the fixed8K gate but not balanced/larger bounds; HZ8 v2 remains public default. Medium-first authority and min/max address-filter follow-ups were measured NO-GO and removed |
| HZ8 native Ubuntu `Page8K-R3` | `GO(opt-in correctness)/NO-GO(performance)` | GCC/Clang smoke and safety passed. Native x86_64 alternating AB/BA R5 measured v2/R3: fixed8K `207.840M/233.532M` (+12.36%), balanced `268.107M/250.363M` (-6.62%), wide `217.539M/217.040M` (-0.23%), larger `113.052M/98.131M` (-13.20%). Redis-like aggregate was `663.320M/660.890M` (-0.37%) with peak RSS `4.852/5.117 MiB` (+5.48%) | Keep R3 as an explicit Linux research/correctness lane. It misses the fixed8K gain gate and exceeds balanced, larger-size, and Redis RSS guards; HZ8 v2 remains the public default |
| HZ10 `BumpInit-L0` / `BumpDefaultGate-L1` | `GO(default)` | RUNS=10 same-run matrix measured `hz10 -> hz10_bump`: `guard_local0` `132.09M -> 238.52M`, `main_local0` `113.91M -> 207.23M`, `main_interleaved_r50` `20.36M -> 21.61M`, and post RSS fell on every measured HZ10 row. Default gate RUNS=3 macro matrix measured no critical regression: `python_alloc` `0.860s/106676KB -> 0.850s/106324KB`, `larson` `4.183s/283008KB -> 4.138s/275192KB`, `xmalloc_test` `2.000s/15104KB -> 2.000s/13568KB`; `sh6bench` was the only small wall regression (`0.420s -> 0.430s`) while RSS improved (`317696KB -> 314240KB`). Bump-enabled public-entry, orphan-partial, retired-local, TSan, ASan/UBSan, RSS guard, and 8s steady-state gates passed | `HZ10_ENABLE_BUMP_INIT=1` is the default. Keep `preload-nobump` as immediate rollback and `preload-bump` as compatibility alias. Correctness requires treating never-bumped slots as free capacity (`free_count` starts at `slot_count`) and using `hz10_freelist_page_has_capacity()` instead of `local_free_head` alone |
| HZ6 Ubuntu remote route resolver | `GO(development lane)` | 2026-06-19 Linux x86_64 selected RUNS=10 median baseline: `remote50=763130.83 ops/s`, `remote90=148892.40 ops/s`; integrity smoke kept unproven real-free, resolver retry/integrity, remote compaction attempt, fallback, and overflow counters at zero | Keep the shared-first resolver and integrity gate as the Ubuntu selected development baseline; Windows x64 still needs its own confirmation before cross-platform promotion |
| `hz4` mid line | `FROZEN` | B70 promoted `HZ4_MID_PAGE_SUPPLY_RESV_CHUNK_PAGES=16`; the remaining live knob `HZ4_MID_FREE_BATCH_CONSUME_MIN=2` is still under validation | Mid-line tuning is mostly exhausted; keep the current default as the reference point until the last live candidate is reconciled |
| Windows foreign-pointer safety box | `GO/default` | A Windows foreign-pointer smoke against a reserved+committed helper allocation showed the default path AVing before the boundary fix, while the registry-on build returned `usable_size -> 0`, `free -> returned`, and `realloc -> NULL` across a 10-run smoke; the extra cost shows up on the free-route boundary (`seg_checks=free_calls`) in the small-free-heavy mixed lane | Keep `HZ4_FREE_ROUTE_SEGMENT_REGISTRY_BOX=1` as the Windows HZ4 default safety boundary; keep `=0` only for perf-only rollback |
| Windows one-shot ownership box | `GO/default` | The Windows wrapper/core routing split was tightened so ownership is proved once and then routed once; on `bench_mixed_ws` the 5-run median improved from `361.259M` (`HZ4_WIN_ONE_SHOT_OWNERSHIP_BOX=0`) to `410.164M` (`=1`), and the diagnostic counters dropped `seg_checks` from `2001152` to `0` on the same shape | Keep `HZ4_WIN_ONE_SHOT_OWNERSHIP_BOX=1` in the Windows HZ4 build entrypoints and use `=0` only as a rollback / bisect lane |
| Linux arm64 preload ownership fix | `GO/default` | The 2026-03-21 Linux arm64 follow-up fixed the foreign-free crash reproducer and restored the rebuilt default lane to an 8-run median of `268.0M ops/s` | Keep this as the Linux default preload safety fix; it is a Linux overlay change, not a Windows/macOS promotion |
| Linux arm64 `hz4` order-gate on mixed compare | `NO-GO` | The same 2026-03-21 follow-up made the lane stable again, but its 8-run median (`218.8M ops/s`) stayed well below the rebuilt default lane (`268.0M ops/s`) | Keep it experimental and opt-in; if work continues, treat it as a throughput-only line |
| `B70 chunk16` | `GO/default` | Canonical Larson and MT remote reruns on the live `chunk16` default | Current live default for the segment-registry lane set |
| `S142 lock-free MPSC` | `GO` | S142 work order marked GO; MT remote results improved materially with collision-failfast discipline | Keep `S86` shadow off when using the lock-free lane |
| `S56 active segment set` | `NO-GO` | `S56-1B` and `S56-2` results did not move RSS enough and/or regressed mixed | Keep it opt-in only if the research direction changes |
| `S236 aggressive micro-family` | `FROZEN` | `S236-N` promoted; `S236-K`, `S236-O`, `S236-P`, `S236-Q` are NO-GO | The micro-family is mostly exhausted; new win candidates are not obvious |
| `p32 build presets` | `GO(tooling)` | `all_ldpreload_scale_p32_96/128/255` now do clean rebuilds and `clean` removes the p32 out dir and libs | Use the preset targets for reproducible p32 A/B runs |
| Mac benchmark lane | `GO(development lane)` | Mac benchmark docs and runs are now public | Treat Mac as a separate development lane, not a released Linux/Windows lane |
| `hz4` `malloc-large` large-path box | `GO(development lane)` | The stats-enabled Mac capture keeps `hz4` as the clear outlier on `malloc-large` (`2.698s` median vs `0.796s` system, `0.875s` mimalloc, `0.914s` tcmalloc) and `ext_bytes_peak` plateaued at `256MiB` | Keep this as a dedicated large-path investigation, but treat `HZ4_LARGE_EXTENT_CACHE_MAX_PAGES=400` / `HZ4_LARGE_EXTENT_CACHE_MAX_BYTES=1GiB` as a no-go combo for now and look for a narrower hypothesis |
| Mac foreign-pointer safety lane | `GO/default` | The Mac foreign-pointer helper boundary is now baked into the default Mac release and observe lanes, and the same canonical paper-suite workloads still ran cleanly after the routing split | Keep the helper boundary in place as the default path; retain the explicit wrapper only as a rollback/compatibility alias if needed |
| Segment-registry high-remote fallback box | `GO(tooling)` | The stats-enabled 90% remote A/B still shows fallback pressure, and the `65536` slot variant was worse than `32768` (`12.92M` vs `11.76M ops/s`) | Treat ring pressure as bench-conditioning first; keep `slots=32768` as the current reference and do not promote the larger slot table on this lane |
| Mac core/overlay split | `GO` | `hz3/hz4` profiling plus the direct `mimalloc` pass all point to shared allocator costs versus Mac glue costs | Keep allocator logic shared; keep dyld interpose, zone fallback, and profiling recipes in the Mac overlay |
| Mac allocator fork | `NO-GO` | The current evidence points to routing / sync / ownership cost, not to a need for a separate Mac allocator family | Do not split `hz3/hz4` into Mac-only allocator implementations |
| Mac build-flags-only theory | `NO-GO` | The runner already uses the normal Mac release flow, and hot-path traces point at allocator code shape instead of a compiler-only problem | Treat build flags as secondary until the hot path itself is simpler |
| `hz3` weak-on-Mac theory | `NO-GO` | A fresh 2026-03-19 rebuild and rerun restored `hz3` to `Larson median=170.6M ops/s` and `MT remote median=102.1M ops/s`, and the observe lane showed no `HZ3_NEXT_FALLBACK` traffic on canonical Larson | Do not treat the earlier weak Mac `hz3` numbers as allocator truth unless the old artifact path can be reproduced |
| `hz3` current-segment medium fast path | `GO` | The shared-core `hz3_slow_alloc_from_segment_locked()` fast path lifted serial Mac medians to `Larson=190.4M ops/s` and `MT remote=119.1M ops/s` | Keep it as the new current-tree reference and continue to treat Larson and MT remote as separate `hz3` boxes |
| `hz4` segment-registry `slots=65536` | `GO` | Current-source Mac A/B reduced `cross64_r90` registry misses and improved the release median from `18.2M` to `19.9M ops/s` | Treat it as a cross/high-remote specialist box; `main_r50` stayed roughly flat so do not blanket-promote it yet |
| `hz4` B37 current-tree `PREV_SCAN_MAX=2` | `GO` | Current-tree sweep on the live `chunk16` baseline lifted `cross64_r90` from `17.9M` to `23.8M ops/s` while `main_r50` stayed below the default | Treat it as a cross/high-remote specialist box; keep it separate from the next Larson mid search |
| `hz4` B37 current-tree `PREV_SCAN_MAX=1` | `NO-GO` | The same current-tree sweep regressed both `main_r50` and `cross64_r90` versus the live default | Do not reuse the scan1 cutoff |
| `hz4` guarded free-batch consume box | `GO(tooling)` | `HZ4_MID_FREE_BATCH_CONSUME_SC_MAX` is now implemented in shared core and defaults to all mid classes | Use it for narrow A/B on Mac / Linux / Windows without forking allocator logic |
| `hz4` `MIN=2` + `SC_MAX=127` specialist tuple | `NO-GO` | The first Mac specialist check regressed `main_r50` materially even though `cross64_r90` held up | Keep the guard mechanism, but do not reuse this exact cutoff tuple |
| Direct `mimalloc` Mac profiling | `GO(tooling)` | A canonical Larson pass was captured on Mac and now gives stack-level evidence instead of benchmark-only inference | Use it when comparing `hz3/hz4` against Mac-friendly allocator behavior |
| HZ6 Ubuntu `PreloadForeignResolvedDispatch-L1` | `GO/default` | RUNS=10 selected remote median improved `remote50` from `763130.83` to `14351051.48` ops/s and `remote90` from `148892.40` to `422566.06` ops/s; integrity smoke showed `free_resolved_foreign_direct_dispatch=39715`, duplicate route lookup avoided at the same count, and zero unresolved/real-free/remote-compaction gates | Keep `HZ6_PRELOAD_FOREIGN_RESOLVED_DISPATCH_L1=1` in the Ubuntu selected preload lane; it is foreign-only after preload active maps miss, so local/mixed rows stay on the existing selected path |
| HZ6 Ubuntu `RemoteFreeCommitObserve-L1` | `GO(tooling)` | Integrity smoke split the current remote-free path into `remote_free_foreign_candidate=39715`, `transfer_reserve_success=893`, `transfer_reserve_full=38822`, `route_rehome_commit_success=893`, and `remote_free_returned_uncommitted=38822` | Treat `route_rehome_attempt >> route_rehome_success` as transfer/backpressure attrition, not proof of excessive committed rehome; next box is remote-free commit transaction/backpressure |
| HZ6 Ubuntu `RemoteFreeCommit-L1` reserve-first | `GO(correctness)/HOLD(perf)` | Selected integrity smoke now gates `transfer_reserve_full_after_state_mutation=0`; quick RUNS=3 stayed strong on `remote50` (`13971677.43` ops/s) but `remote90` fell to `257756.17` ops/s versus the prior `422566.06` RUNS=10 median | Keep the reserve-before-descriptor-mutation invariant, but do not treat this as a completed performance win; next work should address backpressure/drain before RUNS=10 promotion |
| HZ6 Ubuntu `RemoteFreeBackpressure-L1` transfer capacity 256 | `GO/default` | RUNS=10 selected remote median held `remote50` at `14363938.00` ops/s and lifted `remote90` to `6610576.93` ops/s; integrity smoke kept `transfer_reserve_full_after_state_mutation=0`, though `transfer_reserve_full=31316` still shows remaining backpressure | Keep the 256 transfer cap/profile capacity in the Ubuntu selected preload lane; continue with explicit backpressure/drain because capacity alone does not eliminate full events |
| HZ6 Ubuntu transfer capacity 512/1024 ladder | `NO-GO` | Quick RUNS=3 showed 512 regressing `remote90` to `382901.85` ops/s and 1024 regressing it to `1949951.84` ops/s; smoke showed committed rehome/tombstone pressure rising (`route_rehome_success=5099` at 512, `10737` at 1024) | Do not raise selected capacity above 256 without a separate rehome/tombstone mitigation; the next box should reduce backpressure without increasing committed rehome debt |
| HZ6 Ubuntu `RemoteFreeBackpressureDrain-L1` | `GO(remote90-specialist)/HOLD(default)` | Opt-in RUNS=10 moved `remote90` from `6610576.93` to `6969804.00` ops/s and smoke reduced final `transfer_reserve_full` to `10768` with `remote_free_backpressure_retry_success=24206`, but `remote50` dropped from `14363938.00` to `13321713.54` ops/s | Keep the implementation and counters, but leave the flag off in selected preload until the drain is gated to high-remote pressure or avoids extra committed rehome work |
| HZ6 Ubuntu `RemoteFreeBackpressureDrain-L1` gated controls | `HOLD(default)` | `STRIDE=2` failed to hold in RUNS=10 (`remote50=13208613.68`, `remote90=6434072.00`); `MAX_FRONTCACHE_COUNT=4` gave a smaller tradeoff (`remote50=14180156.45`, `remote90=6926930.38`) and passed smoke, but still regressed remote50 versus the capacity-256 selected baseline | Keep `DRAIN_STRIDE` and `DRAIN_MAX_FRONTCACHE_COUNT` as opt-in A/B controls; do not select until the gate produces a no-regression remote50 result or the row is explicitly treated as a remote90 specialist |
| HZ6 Ubuntu `RemoteFreeBackpressureObserve-L1` occupancy counters | `GO(tooling)` | Selected smoke showed reserve full occurs at effectively full transfer capacity: `transfer_reserve_full=33465`, `transfer_reserve_full_transfer_count_total=8567040` (avg 256), `transfer_reserve_full_class_count_total=5123433` (avg 153), max same-class count 203 | Treat remaining backpressure as a true global transfer-capacity saturation problem; next box should use a bounded overflow/pending policy instead of more eager drain/rehome |
| HZ6 Ubuntu `RemoteFreeOverflow-L1` | `NO-GO(default)` | Opt-in smoke passed, but quick RUNS=3 was weak: cap64 `remote50=13789256.22`, `remote90=6044836.89`; cap256 `remote50=14399762.89`, `remote90=2440102.22`. Cap256 committed `remote_free_overflow_reserve_success=3038` but raised `route_rehome_success=6668` | Keep the opt-in research box for correctness experiments, but do not select it for performance; it converts backpressure into extra route movement instead of removing the rehome bottleneck |
| HZ6 Ubuntu `RemoteFreeConsumerRehome-L1` | `NO-GO(default)` | Opt-in smoke passed and shifted route transfer to reuse (`transfer_reuse_rehome_success=2183`, `route_tombstone_current=868`), but quick RUNS=3 regressed to `remote50=12638884.18`, `remote90=269255.48` | Do not move route rehome to the hot transfer reuse path; the next route-movement reduction needs a cheaper proof/token path or a true pending logical-owner model |
| HZ6 Ubuntu shared-route transfer probe observe | `GO(tooling)` | Selected smoke showed shared-directory owner transfer is already cheap: `route_rehome_directory_transfer_probe_total=2661` for `route_rehome_success=2627`, max probe 3 | Do not prioritize proof-token transfer as the next performance box; focus on reducing committed route movement, lock hold work, or tombstone debt |
| HZ6 Ubuntu route rehome probe observe | `GO(tooling)` | Selected smoke split committed rehome probes and found all local phases short: `origin_lookup_total=2522`, `destination_register_total=2504`, `origin_unregister_total=2522` for `route_rehome_success=2465`, max 2-3; quick RUNS=3 guarded at `remote50=13177127.52`, `remote90=7498798.29` | Route-table probe chains inside committed rehome are not the current primary bottleneck; continue toward explicit backpressure/route-movement policy rather than proof-token or local exact-table probe shortening |
| HZ6 Ubuntu `RemoteFreeCommitStatus-L1` | `GO(tooling)` | Selected smoke classified false remote-free commits as backpressure: `remote_free_status_committed=3404`, `remote_free_status_backpressure=36311`, `remote_free_status_stale=0`, `remote_free_status_integrity_failure=0`; quick RUNS=3 guarded at `remote50=14214014.66`, `remote90=6654842.21` | Keep the bool wrapper for current fronts, but use the new status boundary for the next policy box so BACKPRESSURE is not merged with stale or integrity failures |
| HZ6 Ubuntu `RemoteFreeStatusDispatch-L1` | `GO(tooling)` | Selected smoke showed transfer-based remote frees now use the status boundary: `remote_free_status_dispatch_transfer=34757`, `remote_free_status_committed=3227`, `remote_free_status_backpressure=31530`, stale/integrity zero; quick RUNS=3 guarded at `remote50=14969255.96`, `remote90=6625844.88` | The selected Toy/MidPage/Local2P remote-free caller path can now inspect BACKPRESSURE directly; Large remains on bool fallback until it needs a separate policy box |
| HZ6 Ubuntu `RemoteFreeBackpressurePolicy-L1` | `GO(tooling)` | Selected smoke now splits policy outcomes: `remote_free_returned_backpressure=36383`, `remote_free_returned_uncommitted=0`, `remote_free_returned_stale=0`, `remote_free_returned_integrity_failure=0`; quick RUNS=3 guarded at `remote50=14743023.84`, `remote90=5656564.99` | Treat `remote_free_returned_backpressure` as the next behavior target; stale/integrity/uncommitted returns are now zero-gated separately |
| HZ6 Ubuntu `RemoteFreeBackpressureOriginTransfer-L1` | `GO/default` | Historical stride-2 candidate held `remote50=14934275.07` and lifted `remote90=9902231.78`, but later `BackpressurePolicyClock-L1` showed that diagnostic stats had been part of the cadence | Keep `HZ6_REMOTE_FREE_BACKPRESSURE_ORIGIN_TRANSFER_L1=1`; current selected stride is recorded by the policy-clock row below |
| HZ6 Ubuntu `BackpressurePolicyClock-L1` | `GO(default)` | Policy cadence no longer uses diagnostic-only `transfer_reserve_attempt`; true `STRIDE=2` RUNS=10 measured `remote50=14090990.96`, `remote90=2134217.85`, while selected true `STRIDE=1` quick RUNS=3 measured `remote50=15149047.54`, `remote90=8788405.81` | Select `HZ6_REMOTE_FREE_BACKPRESSURE_ORIGIN_TRANSFER_STRIDE=1` so production and diagnostic builds run the same origin-transfer policy; use the new policy clock for future pending-inbox work |
| HZ6 Ubuntu `RemotePendingInboxCore-L1` | `GO(core)/HOLD(behavior)` | Selected/off build and integrity smoke passed with all `remote_pending_*` counters at zero; opt-in/on smoke also passed with `HZ6_REMOTE_PENDING_INBOX_CORE_L1=1` and no enqueue/maintenance activity because no behavior path is connected yet | Keep the per-class owner inbox core behind an explicit flag; next box is `RemoteFreeBackpressureOwnerInbox-L1`, which should publish destination-transfer-full fallback to the origin owner inbox instead of mutating origin transfer/frontcache state from the remote thread |
| HZ6 Ubuntu `RemoteFreeBackpressureOwnerInbox-L1` | `GO(experimental)/HOLD(default)` | Opt-in smoke published `remote_free_origin_pending_commit=33196` with `remote_free_pending_publish_fail=0`, zero pending mismatch gates, and RUNS=3 measured `remote50=14988073.96`, `remote90=10816334.11` | Strong candidate, but not selected yet: `remote_pending_current` remains high at exit and `remote_free_returned_backpressure=110`; next box should improve owner-local pending consumption before RUNS=10 promotion |
| HZ6 Ubuntu `RemotePendingReuseDemandAudit-L1` | `GO(tooling)` | Opt-in owner-inbox smoke showed `remote_pending_key_nonempty_load=6478`, `remote_pending_key_nonempty_hit=1`, `source_alloc_with_matching_pending=0`, with zero publish/uncommitted gates; after replacing a scan-based mask refresh with O(1) per-key counts, RUNS=3 measured `remote50=14637714.57`, `remote90=10727161.65` | The current smoke's large pending balance is not yet proof of missed same-key reuse demand; direct reuse should key off the O(1) mask and source-allocation demand counters rather than trying to drain pending to zero |
| HZ6 Ubuntu `RemotePendingExactKeyClaimCore-L1` | `GO(core)/HOLD(perf)` | Opt-in smoke passed with per-front heads and slot state active but direct reuse disconnected: `remote_pending_queued_current=29115`, `remote_pending_claimed_current=0`, mismatch gates zero; quick RUNS=3 measured `remote50=12536762.73`, `remote90=9837030.86` | Keep as a prerequisite for direct reuse, not a performance selection; next box should move the audit point before maintenance and cover source prefill before wiring the direct-reuse caller |
| HZ6 Ubuntu `RemotePendingReuseDemandAuditV2-L1` | `GO(tooling)` | Opt-in smoke found same-key demand before existing maintenance (`pending_same_key_before_maintenance=920`) plus immediate reuse through the old batch path (`pending_maintenance_immediate_reuse_success=975`, `pending_maintenance_batch_surplus=2569`); source-block commits also overlap pending (`source_block_commit_with_matching_pending=102`); quick RUNS=3 measured `remote50=13570506.65`, `remote90=10002012.48` | V1 under-observed the opportunity because it ran after maintenance; DirectReuse is worth wiring as a default-off replacement for the existing pending->frontcache->pop sequence, but selection remains HOLD until the behavior box is measured |
| HZ6 Ubuntu `RemotePendingDirectReuse-L1` | `GO(experimental)/HOLD(default)` | Direct-only smoke claimed and activated `1453` pending entries with no integrity failures, but RUNS=3 collapsed `remote90` to `1423660.43`; direct+maintenance smoke claimed `3065` entries, kept old batch work to `480`, and RUNS=3 measured `remote50=14162158.67`, `remote90=11188549.20` | Do not use direct reuse as the only consumer; keep it as an opt-in short-circuit before owner-local maintenance, and tune the fallback/maintenance policy before default promotion |
| HZ6 Ubuntu `RemotePendingExactKeyMaintenance` | `GO(correctness)/HOLD(perf)` | Maintenance now drains only the caller's `(front_id,class_id)` key; opt-in smoke showed `remote_pending_direct_claim_success=3392`, `remote_pending_batch_items=71`, mismatch gates zero; quick RUNS=3 measured `remote50=14252255.99`, `remote90=10071818.95` | Keep the exact-key boundary; it removes class-only fallback ambiguity before any DirectReuse promotion, but broader RUNS=10 evidence is still needed |
| HZ6 Ubuntu DirectReuse fallback budget1 | `GO(opt-in)/HOLD(default)` | Direct fallback budget 1 removed surplus in smoke (`remote_pending_batch_items=18`, `pending_maintenance_batch_surplus=0`, integrity zero). RUNS=10 measured selected/off `remote50=15033261.62`, `remote90=10196391.35`; opt-in direct+exact budget1 `remote50=14436403.64`, `remote90=10810219.46` | Keep budget1 for the opt-in DirectReuse lane, but do not promote by default because remote50 still regresses versus selected |
| HZ6 Ubuntu `RemotePendingPhaseReuseBench-L1` | `GO(tooling)` | Added `hz6_allocator_bench phase-reuse`, runner knobs, selected-flag benchmark builds, and remote-pending bench counters. Selected-shape owner-inbox diagnostic run at `phase-reuse speed 512 128` showed `foreign origin_pending_commit=256`, `origin enqueue_success=256`, `origin batch_items=256`, `reuse_hits=256`, and `direct_gate_load=0` | The harness proves same-key owner-inbox demand and generic owner-local maintenance consumption. It does not exercise preload-only DirectReuse; extending direct pending reuse into generic `hz6_malloc()` is the next behavior/design decision |
| HZ6 Ubuntu `PreloadPhaseReuseTarget-L1` | `GO(tooling/evidence)` | Added `bench_phase_reuse` and `run_hz6_preload_phase_reuse.sh` to exercise the preload malloc/free hooks directly. RUNS=3 at `threads=4 count=2048 size=128` measured selected median `664078.071 ops/s` with `reuse_hits=256`, direct median `694969.009 ops/s` with `reuse_hits=1024`; direct stats showed `remote_pending_direct_claim_success=1024`, `remote_pending_direct_integrity_failure=0`, and `remote_pending_batch_items=0` | DirectReuse works for the intended preload phase-shift workload. Keep default promotion separate because random remote RUNS=10 still showed remote50 regression |
| HZ6 Ubuntu `DirectReuseHotPathShape-L1` | `GO(shape)/HOLD(default)` | Production DirectReuse now compiles attribution writes behind `HZ6_REMOTE_PENDING_DIRECT_OBSERVE_L1`; phase smoke at `threads=4 count=2048 size=128` kept direct `reuse_hits=1024` while production direct emitted `remote_pending_direct_gate_load=0`, and direct-stats emitted `claim_success=1024`, `integrity_failure=0`, `batch_items=0` | Behavior is unchanged but cheaper to A/B: one helper owns the preload DirectReuse boundary, known-nonempty claim avoids the second mask check, and integrity failure aborts instead of falling through. Next box is cost attribution or source-demand gate, not default promotion |
| HZ6 Ubuntu `DirectReuseCostAttribution-L1` | `GO(tooling)` | Added a P0-P3 runner. RUNS=3 measured `p1_inbox` vs `p2_gate` roughly neutral on remote50 (`14.23M` -> `14.37M`) while `p3_claim` fell to `11.66M`; raw output lives under `hakozuna-hz6/private/raw-results/linux/hz6_direct_reuse_cost_ab_20260620_r3` | The remaining DirectReuse cost is mainly claim + exact route validation + activation/reuse ordering, not the compiled mask gate. Next behavior box should be `DirectReuseSourceDemandGate-L1`, placing claim at the source boundary rather than frontcache miss |
| HZ6 Ubuntu `DirectReuseSourceDemandGate-L1` | `GO(phase-specialist)/NO-GO(default)` | Source-gate phase RUNS=3 kept `reuse_hits=1024` and matched direct (`717837.45` vs `714605.86` ops/s); diagnostic source-gate smoke proved `source_boundary_claim_success=2303`, `claim_before_existing_reuse=0`, frontcache/transfer nonempty claim gates zero, and integrity zero | Do not select by default: random remote RUNS=3 regressed to `p4_source_gate remote50=13.75M`, `remote90=1.08M`, with large pending inventory because pre-source owner maintenance is disabled. Next work needs a hybrid cleanup consumer, not source gate alone |
| HZ6 Ubuntu source-gate hybrid cleanup | `GO(phase-specialist)/NO-GO(default)` | Hybrid source gate keeps phase reuse at `reuse_hits=1024` with `source_boundary_claim_success=1024` and no batch work, but RUNS=10 regressed versus selected: `p0_selected remote50=15.02M`, `remote90=10.21M`; `p4_source_gate_hybrid remote50=13.85M`, `remote90=9.99M` | Keep as phase/research only. Restoring exact-key maintenance after source-boundary misses is not enough for random remote default; the next default line should avoid source-gate claim on random remote or find a separate consumer policy |
| HZ6 Ubuntu origin-transfer fail reason observe | `GO(tooling)` | Selected smoke split origin-transfer misses into `remote_free_backpressure_origin_transfer_stride_skip=16295`, `validation_fail=0`, and `full=11311`; quick RUNS=3 guarded `remote50=14823669.53` but `remote90=1689959.38` | The remaining origin fallback failures are transfer-cache full, not ownership validation; next behavior work should target origin transfer capacity/stride/backpressure policy while treating remote90 as still open |
| HZ6 Ubuntu origin-transfer full occupancy observe | `GO(tooling)` | Selected smoke showed origin full at global capacity: `origin_transfer_full=31298`, `origin_full_transfer_count_total=8012288` (avg 256), `origin_full_class_count_total=4785345` (avg 153), max same-class count 200; destination full showed the same avg 256 shape | Treat origin-transfer failures as true transfer-cache saturation, not class-only imbalance. Class/shard tuning alone is unlikely to close this; next behavior needs a cheaper owner-local consumer or guaranteed owner-owned sink |
| HZ6 Ubuntu owner-inbox RUNS=10 recheck | `GO(high-remote-specialist)/HOLD(default)` | Rechecking the existing owner-inbox lane measured selected `remote50=14.76M`, `remote90=1.22M` versus owner-inbox `remote50=14.03M`, `remote90=9.94M`; smoke committed `remote_free_origin_pending_commit=35243` with pending mismatch/fail gates zero, but left `remote_free_returned_backpressure=11` | Owner-inbox is the strongest high-remote recovery path, but do not select by default until the remote50 cost and small backpressure tail are addressed |
| HZ6 Ubuntu `OwnerInboxRejectReasonObserve-L1` | `GO(tooling)` | Owner-inbox smoke split rejects: `storage_ineligible=2897`, descriptor/owner/enqueue failures all zero; the remaining tail was `origin_transfer_full=45`, `remote_free_returned_backpressure=45` | The tail is external descriptor coverage, not owner mismatch or queue failure. Next correctness box should be an external-descriptor owner-inbox ticket path |
| HZ6 Ubuntu `ExternalDescriptorOwnerInboxTicket-L1` design | `GO(design)/HOLD(implementation)` | `HZ6_REMOTE_PENDING_INBOX_PLAN.md` now defines a separate owner-owned ticket queue for external descriptors, with immutable proof fields and zero gates for route/owner/state/storage mismatch | Implement as separate ticket storage, not pseudo descriptor indices in the inline pending arrays; promotion still requires `remote_free_returned_backpressure=0` and separate remote50 cost work |
| HZ6 Ubuntu `ExternalDescriptorOwnerInboxTicketCore-L1` | `GO(core)/HOLD(behavior)` | Added default-off external ticket storage, per-key heads, free-list init, and counters. Selected/off build, external-ticket-core build, selected-shape benchmark build, and smoke passed; ticket counters remain zero because no publish path is connected | Next box can publish storage-ineligible owner-inbox candidates into the ticket queue without changing the inline descriptor-index queue |
| HZ6 Ubuntu `ExternalDescriptorOwnerInboxTicketPublishAPI-L1` | `GO(API)/HOLD(behavior)` | Added default-off producer-facing publish API and dedicated external ticket lock. Selected/off build, external-ticket publish build, selected-shape benchmark build, and smoke passed; ticket counters remain zero because free path is still disconnected | Connect only after owner-local consume/destroy handling is ready; selected-shape builds without descriptor-storage-owner lookup fail publish with storage mismatch by design |
| HZ6 Ubuntu `ExternalDescriptorOwnerInboxTicketConsumeAPI-L1` | `GO(API)/HOLD(behavior)` | Added disconnected owner-local `consume_one(front,class)` primitive. Selected/off build, external-ticket consume build, selected-shape benchmark build, and smoke passed; ticket counters remain zero because runtime is still disconnected | Producer and consumer primitives now exist; next behavior box can connect storage-ineligible owner-inbox publish and owner-local maintenance together |
| HZ6 Ubuntu `ExternalDescriptorOwnerInboxTicketMaintenance-L1` | `GO(consumer)/HOLD(producer)` | Owner-local pending maintenance now arms on external ticket exact-key heads and tries external consume before inline pending pop. Selected/off build, external-ticket maintenance build, benchmark build, and smoke passed with ticket counters still zero | Consumer side is ready; next box may connect only storage-ineligible owner-inbox candidates to ticket publish |
| HZ6 Ubuntu `ExternalDescriptorOwnerInboxTicketPublish-L1` | `GO(correctness)/HOLD(default)` | Storage-ineligible owner-inbox candidates now publish external tickets. Smoke closed the tail with `remote_free_returned_backpressure=0`, `external_ticket_success=2214`, `full/duplicate/mismatch=0`; quick RUNS=3 measured selected `remote50=14.48M`, `remote90=8.63M` vs external ticket `remote50=13.62M`, `remote90=10.45M` | Correctness coverage is good, but default selection still waits because remote50 regresses and external-ticket backlog remains visible |
| HZ6 Ubuntu `ExternalTicketObserveAndConsumeGate-L1` | `GO(tooling+shape)/HOLD(default)` | Added external-ticket current/high-water/empty/frontcache-full counters, then gated maintenance so it only calls external consume when the exact key is nonempty. Smoke kept `remote_free_returned_backpressure=0`, reduced `external_ticket_consume_empty` from `3750` to `0`, and showed `external_ticket_current=1970`, `high_water=269`; quick RUNS=3 measured `remote50=14.14M`, `remote90=8.80M` | The empty-consume cost is removed, but high remote selection is still HOLD: backlog remains and quick perf did not beat the prior external-ticket sample |
| HZ6 Ubuntu `RemotePendingMaintenanceGateShape-L1` | `NO-GO` | Tried collapsing owner-local maintenance entry probes so inline/external exact-key nonempty checks were computed once. Smoke stayed clean (`remote_free_returned_backpressure=0`, `external_ticket_consume_empty=0`), but quick RUNS=3 was weaker at `remote50=13.78M`, `remote90=8.77M`; code was reverted | Do not revisit this as a standalone hot-path shape tweak; next work needs a larger consumer/backlog change with direct performance evidence |
| HZ6 Ubuntu `OwnerInboxDrainBudget1-L1` | `GO(opt-in shape)/HOLD(default)` | Lowered the owner-local pending maintenance default drain budget from 4 to 1. Override A/B smoke stayed clean and measured `remote50=14.35M`, `remote90=10.48M`; committed default smoke kept `remote_free_returned_backpressure=0`, `external_ticket_consume_empty=0`, and quick RUNS=3 measured `remote50=14.41M`, `remote90=9.47M` | Keep budget1 as the owner-inbox opt-in shape because it avoids surplus staging, but default selection still waits on broader owner-inbox cost/backlog evidence |
| HZ6 Ubuntu `ExternalTicketDemandAudit-L1` | `GO(tooling)` | Diagnostic `remote_pending_key_nonempty` now includes external tickets as well as inline pending. Owner-inbox+external smoke stayed clean and showed `remote_pending_key_nonempty_hit=7424/21435`, `pending_same_key_before_maintenance=3688`, `source_block_commit_with_matching_pending=73`, `source_alloc_with_matching_pending=0`, `external_ticket_current=1920` | Backlog is not purely cold inventory: same-key demand still reaches source-block commit. Next design checkpoint should target source-block commit/pre-source consumer, not direct-source fallback |
| HZ6 Ubuntu `PreSourcePendingMaintenance-L1` | `NO-GO` | Tried one owner-local pending maintenance pass after source-run reuse misses and before creating a new source block. Smoke was clean and produced `pre_source_success=48`, but `source_block_commit_with_matching_pending` stayed high at `120`; quick RUNS=3 regressed to `remote50=13.80M`, `remote90=1.71M`; code was reverted | Do not put pending maintenance in the generic source-block creation path. It is too late/too broad and damages remote90 |
| HZ6 Ubuntu `ExternalTicketDuplicateScanObserve-L1` | `GO(tooling)` | Added `remote_pending_external_ticket_duplicate_probe_total/max`. Owner-inbox+external smoke showed `external_ticket_attempt=2321`, `duplicate=0`, but `duplicate_probe_total=2376704` and `duplicate_probe_max=1024`, proving every successful external publish scanned the whole 1024-ticket store | The scan is a real producer-side cost candidate; the next opt-in shape is locked descriptor revalidation before publish instead of a full duplicate scan |
| HZ6 Ubuntu `ExternalTicketLockedRevalidate-L1` | `GO(observation)/NO-GO(default)` | Default-off `HZ6_REMOTE_PENDING_EXTERNAL_LOCKED_REVALIDATE_L1=1` revalidates descriptor proof under the external-ticket lock and skips the duplicate scan. Smoke kept `remote_free_returned_backpressure=0`, `remote_free_returned_uncommitted=0`, mismatch gates zero, `duplicate_probe_total=0`, `scan_skip=2886`, `locked_revalidate_fail=0`; RUNS=10 was weaker than baseline: baseline `remote50=14.17M`, `remote90=10.50M`, locked `remote50=13.80M`, `remote90=9.89M` | Keep the counters and default-off switch for diagnostics, but do not pursue locked revalidation as the next optimization line |
| HZ6 Ubuntu `PendingMaintenanceNoopObserve-L1` | `GO(tooling)/HOLD(policy)` | Added maintenance no-op reason counters. Owner-inbox+external+demand-audit smoke showed `maintenance_check=3688`, `armed=3688`, `noop=0`, `key_race=0`, `external_miss=0`, `inline_empty=0`, `frontcache_full_stop=0`, `batch_items=3688`, but `pending_same_key_after_maintenance=3522` and `source_block_commit_with_matching_pending=126` | Maintenance is not wasting calls; budget1 drains useful work but leaves same-key inventory. Do not tune budget from noisy R3; next work should be demand-shaped consumption |
| HZ6 Ubuntu `SourceOverlapSplitObserve-L1` | `GO(tooling)` | Split source-boundary matching pending by inline inbox vs external tickets. Owner-inbox+external+demand-audit smoke stayed clean and showed `source_block_commit_with_matching_pending=139`, `source_block_commit_with_inline_pending=137`, `source_block_commit_with_external_pending=14` | Source overlap is dominated by inline owner-inbox pending left after budget1 maintenance; next consumer design should focus on inline exact-key pending, not external ticket policy |
| HZ6 Ubuntu `PostHitExtraDrain-L1` | `NO-GO` | Tried draining one additional exact-key item only after pending maintenance produced the current allocation. Smoke was clean and active (`post_hit_extra_items=2442`, `pending_same_key_after_maintenance=2531`, `source_block_commit_with_matching_pending=116`), but quick RUNS=3 regressed `remote90=3.50M` with `remote50=13.94M` | Code reverted. Do not solve inline pending backlog by staging more frontcache work after a successful allocation |
| HZ6 Ubuntu DirectReuse + maintenance + external recheck | `GO(high-remote candidate)/NO-GO(default)` | Rechecked existing `HZ6_REMOTE_PENDING_DIRECT_REUSE_L1=1` with owner-local maintenance and external tickets enabled. It beat owner-inbox+external baseline on remote90 without remote50 loss: baseline `remote50=14.09M`, `remote90=10.79M`; direct `remote50=14.10M`, `remote90=11.14M`. Against selected/off, it slightly improved remote90 but lost remote50: selected `remote50=15.02M`, `remote90=10.93M`. Smoke stayed clean with `direct_claim_success=4789`, `direct_integrity_failure=0`, `batch_items=376`, `frontcache_push=14`, and no returned backpressure/uncommitted | Keep as the high-remote candidate, not the default selected lane. Further work should reduce the remote50 tax of direct pending-pool reuse |
| HZ6 Ubuntu `DirectReuseTransferNonemptySkip-L1` | `NO-GO` | Tried skipping frontcache-miss DirectReuse when same-class transfer inventory existed. Smoke was clean and active (`direct_skip_transfer_nonempty=2146`, `direct_claim_success=2878`, `direct_integrity_failure=0`), but quick RUNS=3 regressed high-remote badly: `remote50=14.19M`, `remote90=3.70M` | Code reverted. In the preload skip-transfer paths, this gate does not safely hand work to transfer reuse |
| HZ6 Ubuntu `DirectReuseFrontClassObserve-L1` | `GO(tooling)` | Added DirectReuse claim split by front and class. DirectReuse+maintenance+external smoke stayed clean (`returned_backpressure=0`, `returned_uncommitted=0`, `direct_integrity_failure=0`) and showed `direct_claim_success=2479`, split as Toy `57` and MidPage `2422`; class detail was `c3=10`, `c4=458`, `c5=2011` | Current DirectReuse work is overwhelmingly MidPage/class 5 with a secondary class 4 component. The next remote50-tax investigation should be MidPage/class-shaped, not Toy-focused |
| HZ6 Ubuntu `DirectReuseTransferOverlapObserve-L1` | `GO(tooling)` | Added successful DirectReuse overlap counters for cases where same-class transfer inventory was present before the claim. Two clean smokes showed the signal can vary by phase: one had `transfer_success=4/1990`, another had `transfer_success=1384/2866`, mostly MidPage (`1266`) and class 5 (`1074`) with class 4 secondary (`308`) | Do not make a skip/ordering policy from a single smoke. The counters are now sufficient to include transfer-overlap attribution in the next RUNS A/B |
| HZ6 Ubuntu DirectReuse overlap perf recheck | `HOLD` | Same-code RUNS=3 after the overlap counters measured `p1_inbox remote50=14.39M remote90=11.21M` and `p3_claim remote50=14.53M remote90=10.85M` | Still not enough for a new behavior gate: DirectReuse can help remote50 in this sample but loses remote90, while prior RUNS=10 showed the opposite direction. Keep observing with the new counters before another skip/order change |
| HZ6 Ubuntu owner-inbox external candidate recheck | `GO(candidate)/HOLD(default)` | Added cost-runner variants for external tickets. RUNS=10 measured selected/off `remote50=14.55M remote90=3.68M`, owner-inbox external `remote50=14.31M remote90=10.89M`, and direct+external `remote50=13.36M remote90=10.75M`; candidate smoke kept `returned_backpressure=0`, uncommitted/stale/integrity zero | Treat owner-inbox external with DirectReuse off as the current selected candidate. Do not promote DirectReuse+external; it regressed both rows in this R10. Default promotion still needs an explicit selected-flag box and one final selected-vs-candidate run |
| HZ6 Ubuntu `OwnerInboxProductionStatsShape-L1` | `GO(candidate prerequisite)` | Producer-side owner-inbox/external-ticket stats now compile out unless `HZ6_DIAGNOSTIC_PROBES` is enabled, external-ticket validation mismatch now aborts instead of freeing the ticket, and external-ticket zero gates were added to the integrity smoke. Candidate smoke stayed clean with returned backpressure/uncommitted/stale/integrity zero and external mismatch/full/duplicate/abort zero; quick p1 external RUNS=3 measured `remote50=14.72M`, `remote90=10.86M` | This closes the two code-shape prerequisites before selected flags are changed. Next box can cut `OwnerInboxExternalSelected-L1`, then run final selected-vs-candidate and RSS/local guards |
| HZ6 Ubuntu `OwnerInboxExternalSelected-L1` | `GO(branch selected)/HOLD(default release)` | Ubuntu selected flags now enable owner inbox, owner-local maintenance, and external tickets while keeping DirectReuse off. Selected smoke stayed clean with returned backpressure/uncommitted/stale/integrity zero and external full/duplicate/mismatch/abort zero. RUNS=10 selected measured `remote50=13.98M`, `remote90=10.83M` | Keep this as the branch selected candidate because it removes the remote90 cliff. Do not call it final default until RSS/local/lifetime/accounting guards are captured |
| HZ6 Ubuntu `OwnerInboxSelectedGuard-L1` | `GO(tooling)` | Added a selected-preload guard runner for local0/remote50/remote90 with `/usr/bin/time` peak RSS. Initial RUNS=3 branch-selected guard measured `local0=15.62M ops/s / 72.88 MiB`, `remote50=13.44M / 74.62 MiB`, `remote90=11.10M / 77.50 MiB` | Use this as the RSS/local baseline for the owner-inbox selected branch. It does not complete release promotion; lifetime/accounting and paired RSS comparison remain open |
| HZ6 Ubuntu `OwnerInboxAccountingGuard-L1` | `GO(tooling)` | Added a diagnostic-only snapshot checker for inline owner-inbox lists, exact-key counts, slot states, external-ticket queue/free-list membership, claimed-at-quiescence, and ticket/slot descriptor state. Selected integrity smoke now zero-gates the accounting counters and passed with inline/external accounting mismatch, total state mismatch, external free-list/list-cycle/multiple-membership, and claimed-at-quiescence counters all zero | Pending backlog remains allowed when accounting is explainable. Default release is still HOLD on allocator lifetime closeout and paired selected-vs-baseline RSS/local guards |
| HZ6 Ubuntu `AllocatorLifetimeCloseout-L1` | `GO(correctness)` | Added destroy-only pending closeout in a separate 198-line module. Inline pending slots are closed before descriptor destroy; external tickets unregister the origin route and release the descriptor/source through the storage owner. Selected integrity smoke zero-gated dying enqueue, dead owner/storage ticket, route-removed-while-pending, external release fail, and after-destroy pending counters. Default smokes passed, and selected+diagnostic transfer smoke with enlarged stack passed inline and external destroy cases | This closes the allocator lifetime prerequisite for the branch-selected owner-inbox candidate. Default release still waits on paired selected-vs-baseline RSS/local/perf gate |
| HZ6 Ubuntu `PairedRSSDefaultGate-L1` | `GO(tooling)/HOLD(default)` | Added a paired p0/p1 preload runner with peak RSS for local0, remote50, remote90, and cross128_r90. RUNS=3 measured p0 selected/off vs p1 owner-inbox: local0 `16.07M/67.12 MiB` vs `14.68M/72.62 MiB`, remote50 `14.01M/69.38 MiB` vs `12.93M/74.88 MiB`, remote90 `3.02M/99.01 MiB` vs `10.39M/77.50 MiB`, cross128_r90 `1.62M/78.70 MiB` vs `12.19M/72.50 MiB` | Owner-inbox stays a strong high-remote branch-selected candidate, but default release remains HOLD because local0/remote50 regress. Next optimization should reduce the p1 local/remote50 tax or introduce an explicit high-remote profile |
| HZ6 Ubuntu `OwnerInboxProfileSplit-L1` | `GO(profile)/HOLD(default promotion)` | `build_hz6_preload_owner_inbox_external_target.sh` now builds the p1 owner-inbox external profile explicitly, and the paired gate sources p0 from an owner-inbox-off helper while sourcing p1 from the owner-inbox external helper. A brief selected=p0 check reintroduced `remote_free_returned_backpressure`, so branch selected remains the p1 candidate | Use owner-inbox external as the branch-selected high-remote candidate and explicit profile. Release/default promotion remains HOLD until a later tax-reduction box removes the local0/remote50 regression or a new paired gate justifies promotion |
| HZ6 Ubuntu `OwnerInboxTaxAttribution-L1` | `GO(tooling)` | Added a diagnostic-first owner-inbox tax runner. RUNS=1 diagnostic local0 showed p0 `67.12 MiB`, p1 metadata `71.62 MiB`, p1 external `72.75 MiB`; remote50 p1 external closed returned backpressure but showed `external_ticket_duplicate_probe_total=2746368` for `external_ticket_success=2682`. Production local0 smoke measured p0 `16.54M/67.25 MiB` vs p1 external `15.55M/72.50 MiB` | Use this as attribution only, not promotion evidence. Next design target is either fixed metadata/RSS (`OwnerInboxLazyStorage-L1`) or external duplicate-scan cost (`ExternalTicketDuplicateIndex-L1`) |
| HZ6 Ubuntu `ExternalTicketDuplicateIndex-L1` | `GO(correctness+cost-shape)/HOLD(default promotion)` | Added a 190-line external-ticket duplicate index module. Integrity smoke kept `remote_free_returned_backpressure=0` and `duplicate_index_stale=0`, while `external_ticket_success=2036` used only `duplicate_probe_total=2217` with max probe `2`. Focused tax run measured p1 external remote50 with `external_ticket_success=2320`, `duplicate_probe_total=2627`, max probe `3`, stale `0`, replacing the prior multi-million full scan signal | Keep the index enabled in the owner-inbox external profile. This closes duplicate scanning as the primary tax suspect, but p1 remote50 still trails p0 in one-shot production smoke, so default promotion remains HOLD |
| HZ6 Ubuntu `OwnerInboxStorageFootprintAudit-L1` | `GO(tooling)/DESIGN checkpoint` | Added a compile-time footprint audit. p0/off `sizeof(Hz6Allocator)=3251928`; p1/external `sizeof(Hz6Allocator)=3588872`, with `owner_inbox_bytes=336896`: inline slot/proof storage `270336`, external tickets `57600`, duplicate index `8192` | Lazy storage must cover the inline pending slot/proof arrays, not just external tickets. Stop for design before moving this storage because the allocation mechanism must avoid preload `calloc()` recursion |
| HZ6 Ubuntu `OwnerInboxStorageProvider-L1` | `GO(boundary)/HOLD(lazy migration)` | Added a small owner-inbox storage provider that uses OS-backed allocation (`mmap` on Linux, `VirtualAlloc` on Windows) instead of `malloc/calloc`, zero-fills the returned block, and releases with the same rounded size. R1 smokes, preload build, selected integrity smoke, and the footprint audit passed; the footprint stayed p1 `owner_inbox_bytes=336896`, confirming no behavior/storage migration yet | This is the recursion-safe allocation boundary for `OwnerInboxLazyStorage-L1`. Do not move arrays behind it until the next boxed migration updates init/destroy/accounting together |
| HZ6 Ubuntu `OwnerInboxLazyStorage-L1` | `GO(candidate)/HOLD(default promotion)` | Moved owner-inbox inline proof/slot arrays, external tickets, and duplicate index behind the OS-backed storage provider. Footprint audit now shows p1 `sizeof_Hz6Allocator=3251960` versus p0 `3251928` while logical owner-inbox storage remains `336896` bytes and is allocated only on first publish. R1 smokes, owner-inbox external target build, integrity smoke, and production RUNS=3 passed; production p0 vs p1 measured local0 `16.13M/67.38 MiB` vs `16.36M/67.12 MiB`, remote50 `14.56M/69.75 MiB` vs `13.76M/74.88 MiB`, remote90 `3.22M/79.66 MiB` vs `10.79M/77.34 MiB` | Lazy storage closes the fixed local-only RSS tax and the initial production abort was fixed by publishing the lazy storage pointer only after reset under an init lock. Default promotion remains HOLD because remote50 still regresses; next work should target p1 runtime tax, not fixed metadata footprint |
| HZ6 Ubuntu `OwnerInboxRemote50RuntimeTaxObserve-L1` | `GO(tooling)` | Added diagnostic-only maintenance gate counters. A p1 external remote50 diagnostic run showed `maintenance_check=3372`, `maintenance_entry_gate_miss=3871`, `inline_gate_hit=3336`, `external_gate_hit=296`, `external_key_probe=7539`, `external_key_hit=504` | The observed empty external-key checks justified a narrow external-ticket nonempty mask; keep these counters for further p1 runtime tax attribution |
| HZ6 Ubuntu `ExternalTicketNonemptyMask-L1` | `GO(shape)/HOLD(default)` | Added an atomic exact-key mask for external tickets to avoid taking the external-ticket lock when the key is empty. Integrity smoke stayed clean with returned backpressure/uncommitted zero, `external_ticket_consume_empty=0`, and external mismatch/full/duplicate/abort zero. RUNS=10 p0 vs p1 measured local0 `14.09M/67.25 MiB` vs `14.05M/67.31 MiB`, remote50 `12.54M/69.62 MiB` vs `12.21M/74.81 MiB`, remote90 `10.02M/72.04 MiB` vs `9.70M/77.24 MiB` | Keep the mask because it removes an empty lock path without hurting correctness, but do not promote p1 as default from this sample. Stop for design before adding another owner-inbox consumer policy |
| HZ6 Ubuntu `OwnerInboxHighRemoteProfile-L1` | `GO(profile)/HOLD(default)` | Selected/default flags now force the owner-inbox family off again, while `build_hz6_preload_high_remote_owner_inbox_target.sh` builds the p1 owner-inbox external profile explicitly with DirectReuse off. Selected integrity smoke passed with owner-inbox counters at zero; high-remote guard RUNS=1 passed at local0 `15.99M/67.50 MiB`, remote50 `14.63M/74.62 MiB`, remote90 `10.52M/77.50 MiB` | Treat owner-inbox external as an explicit high-remote profile, not selected/default. This keeps rollback immediate after the p1 default HOLD decision and leaves the next owner-inbox consumer policy outside the default lane |
| HZ6 Ubuntu profile frontier aliases | `GO(tooling)` | Added benchmark aliases for `hz6-high-remote-owner-inbox-target` and `hz6-transfer-class-shard-target` to both the HZ6 preload build alias layer and `bench/lib/bench_common.sh`; both aliases resolve to built DSOs after their target builders run | Existing profile frontier and selected-balance runners can now compare selected/default, high-remote owner-inbox, and transfer class-shard without custom one-off scripts |
| HZ6 Ubuntu profile frontier alias smoke | `GO(evidence)/HOLD(policy)` | Focused RUNS=3 with `hz6,hz6-high-remote-owner-inbox-target,hz6-transfer-class-shard-target` measured: 16_256 selected `59.19M`, high-remote `58.81M`, class-shard `60.94M`; 16_4096 selected `52.18M`, high-remote `55.15M`, class-shard `56.33M`; 1024_4096 selected `71.02M`, high-remote `69.96M`, class-shard `68.37M`; 4096_16384 selected `62.32M`, high-remote `59.43M`, class-shard `61.39M` | Class-shard remains interesting for small/mixed local rows but is not a broad default. High-remote owner-inbox should remain scoped to remote-heavy lanes, not focused local/default profile selection |
| HZ6 Ubuntu `TransferCapacitySweep-L1` | `GO(tooling)/NO-GO(cap512 default)` | Added a selected/default transfer-capacity sweep runner for ops/RSS and optional diagnostic counters. Production RUNS=3 measured cap256 vs cap512: local0 `16.18M/67.25 MiB` vs `14.90M/67.62 MiB`, remote50 `14.66M/69.50 MiB` vs `14.28M/70.00 MiB`, remote90 `10.73M/72.12 MiB` vs `10.63M/72.00 MiB`; diagnostic counters confirmed cap512 still hits full-capacity transfer saturation | Use the runner for future capacity evidence, but do not increase selected/default transfer capacity to 512 from this sample. The remote90 cliff needs a better policy than larger bounded transfer caches |
| HZ6 Ubuntu `TransferClassShardProfile-L1` | `GO(tooling)/NO-GO(default)` | Added default-off `HZ6_PROFILE_TRANSFER_SHARD_CLASS_L1`, class-shard build target, and `run_hz6_preload_transfer_shard_policy_ab.sh` for selected-vs-class-shard ops/RSS plus optional diagnostic counters. R1 smokes and class-shard integrity smoke passed. RUNS=10 measured selected vs class-shard: local0 `16.59M/67.38 MiB` vs `16.19M/67.44 MiB`, remote50 `14.86M/69.50 MiB` vs `15.02M/69.50 MiB`, remote90 `10.88M/72.21 MiB` vs `10.83M/72.15 MiB` | Keep the runner and opt-in target, but do not promote class sharding by default. It slightly helps remote50 in this batch but costs local0 and does not improve remote90 or clearly reduce backpressure in diagnostic samples |
| HZ6 Ubuntu `SmallClassTransferShard-L1` | `GO(remote50 target)/NO-GO(default)` | Added default-off `HZ6_PROFILE_TRANSFER_SHARD_CLASS_MAX_ID=3`, a small-class shard target, aliases, and runner support. R1 smokes plus selected and small-class integrity smokes passed. Focused RUNS=3 measured small-class over selected on `16_256` (`59.27M` vs `58.52M`), `16_4096` (`57.23M` vs `56.51M`), and `4096_16384` (`63.93M` vs `62.68M`), while reducing the broad class-shard loss on `1024_4096`. Remote MT RUNS=10 measured selected vs small-class at local0 `16.17M` vs `15.95M`, remote50 `15.00M` vs `15.29M`, remote90 `10.93M` vs `10.87M`; diagnostic RUNS=3 did not show a backpressure mechanism for the remote50 win (`returned_backpressure` remote50 selected `28706`, small-class `28927`) | Keep as an explicit remote50/profile target. Do not promote to selected/default, and do not stack more transfer-shard policy tweaks without a new design signal because counters did not confirm a transfer-full/backpressure fix |
| HZ6 Ubuntu `OwnerInboxSmallClassComposition-L1` | `GO(tooling)/NO-GO(profile)` | Added `p1_external_small_class` to the owner-inbox tax runner to compose the high-remote owner-inbox profile with `HZ6_PROFILE_TRANSFER_SHARD_CLASS_MAX_ID=3`. Production RUNS=10 measured p1 external vs composed: remote50 `14.12M` vs `13.93M`, remote90 `10.71M` vs `10.71M`; returned backpressure stayed zero in both | Do not add a combined high-remote target. Small-class sharding does not reduce owner-inbox p1 runtime tax; keep the variant only for attribution |
| HZ6 Ubuntu `OwnerInboxMaintenanceStatsShape-L1` | `GO(shape)/NO-GO(default)` | Moved owner-local remote-pending maintenance stats writes behind the existing diagnostic-only `HZ6_REMOTE_PENDING_STAT_*` macros. R1 smokes, selected preload build, owner-inbox external target build, and integrity smoke passed. Production p0 vs p1 RUNS=10 measured local0 `16.21M` vs `16.47M`, remote50 `15.18M` vs `14.07M`, remote90 `10.68M` vs `10.53M`; production p1 maintenance counters now stay zero as expected | Keep the stats shape cleanup because it removes hot-path writes from production owner-inbox maintenance, but it does not close the p1 remote50/remote90 tax. Default remains owner-inbox off |
| HZ6 Ubuntu `OwnerInboxMaintenanceSinkAudit-L1` | `GO(evidence)/NO-GO(maintenance-off)` | Production RUNS=10 compared `p0_off`, `p1_external_no_maintenance`, and `p1_external`: remote50 was `14.65M`, `14.04M`, `13.72M`; remote90 was `10.91M`, `3.09M`, `10.64M`. The no-maintenance variant also raised peak RSS on remote90 (`95.19 MiB`) versus p1 maintenance (`77.43 MiB`) | Owner-local maintenance is not just runtime tax; it is the high-remote sink that keeps p1 remote90 alive. Do not disable or broadly skip maintenance. Future p1 work should reduce per-item consumer cost or improve demand targeting without starving the sink |
| HZ6 Ubuntu `OwnerInboxMaintenanceCostAttribution-L1` | `GO(tooling)/DESIGN checkpoint` | Added diagnostic-only counters for owner-inbox maintenance external attempts/success, inline pop attempts/success, route validations, frontcache push attempts/success, and drained inline/external items. Selected integrity smoke and R1 smokes passed. Diagnostic RUNS=3 p1 external showed remote50 drained inline/external `4149/400`, route validates inline/external `4149/400`; remote90 drained inline/external `1691/20611`, route validates inline/external `1691/20611` | The next behavior box should not be a single broad maintenance throttle. Remote50 tax is mostly inline maintenance, while remote90 survival is mostly external-ticket consumption. Stop for design: prefer a split policy or targeted inline-cost reduction over disabling the sink |
| HZ6 Ubuntu `OwnerInboxMaintenanceShapeObserve-L1` | `GO(tooling)/DESIGN checkpoint` | Added diagnostic-only front/class split for owner-inbox maintenance drained inline/external items. Selected build, owner-inbox external target build, integrity smoke, and R1 smokes passed. Diagnostic RUNS=3 p1 external showed remote50 inline maintenance is mostly MidPage (`5813/6130` in the median run), class5/class4 (`5204/922`); remote90 external maintenance is mostly MidPage (`21011/25296`), class5/class4 (`12493/10393`) | Next behavior should not throttle all inline pending. If we tune p1 further, target MidPage class5/class4 inline maintenance specifically while preserving external class4/class5 consumption for remote90 |
| HZ6 Ubuntu `OwnerInboxMidpageInlineSkip-L1` | `GO(research)/NO-GO(profile)` | Added default-off `HZ6_REMOTE_PENDING_INLINE_MIDPAGE_MIN_CLASS` and p1 external variants that skip inline MidPage class >=5 or >=4 while preserving external-ticket consumption. Production RUNS=3 p1 external vs skip>=5 vs skip>=4 measured remote50 `14.23M`, `13.76M`, `14.28M`; remote90 `10.65M`, `3.78M`, `3.24M`. Diagnostic RUNS=1 confirmed the skip path was active (`inline_policy_skip=1142/1165`). Integrity smoke and R1 smokes passed | MidPage inline maintenance is not optional for high-remote stability. Do not skip class4/class5 inline work in the high-remote profile; the next idea needs to make inline consumption cheaper, not remove it |
| HZ6 Ubuntu `OwnerInboxSplitMaintenancePolicy-L1` | `GO(research)/NO-GO(profile)` | Added default-off `HZ6_REMOTE_PENDING_FRONT_MAINTENANCE_EXTERNAL_ONLY_L1`, an external-only front maintenance API, DirectReuse-independent source-gate maintenance, and a `p1_external_split_maintenance` tax-runner variant. Selected integrity smoke and R1 smokes passed. Production RUNS=3 p1 external vs split measured remote50 `14.11M` vs `12.29M`, remote90 `10.67M` vs `11.00M` | Splitting front maintenance away from inline work can preserve remote90, but it hurts remote50 too much. Keep the box only as a research control; do not add it to the high-remote profile. The next design should not defer all inline work to source boundary |
| HZ6 Ubuntu `RemotePendingRoutePinAudit-L1` | `GO(tooling)/DESIGN checkpoint` | Added diagnostic-only route mutation shadow counters for `route_unregister_while_pending`, `route_replace_while_pending`, and `route_rehome_while_pending`. Selected integrity smoke and R1 smokes passed. Owner-inbox diagnostic RUNS=3 p1 external showed all three counters at zero for remote50 and remote90 while inline route validations still occurred (`3910` remote50, `1563` remote90) | This supports a future `RemotePendingRoutePinTrust-L1` behavior box that skips production route lookup during inline pending maintenance, with diagnostic shadow validation retained. Stop for design before changing correctness authority |
| HZ6 Ubuntu `RemotePendingRoutePinTrust-L1` | `GO(research)/NO-GO(default)` | Added default-off `HZ6_REMOTE_PENDING_ROUTE_PIN_TRUST_L1` and a `p1_external_route_pin` tax-runner variant. Production RUNS=3 p1 external vs route-pin measured local0 `15.20M` vs `16.61M`, remote50 `13.17M` vs `14.00M`, but remote90 `3.94M` vs `2.71M` with higher RSS. A focused remote90 RUNS=10 recheck still favored p1 external: `3.97M/82.42 MiB` vs route-pin `3.60M/87.64 MiB`. Selected build, owner-inbox target build, integrity smoke, and R1 smokes passed | Skipping inline pending route lookup is not a promotion path now. Keep as a research switch only; route-pin may help lower-remote rows, but it damages the high-remote sink this profile exists to protect |
| HZ6 Ubuntu `OwnerInboxDirectReuseProfileRecheck-L1` | `GO(profile candidate)/HOLD(default)` | Added owner-inbox tax-runner variants for `p1_external_direct_reuse` and `p1_external_direct_reuse_observe`. Production RUNS=10 measured p1 external vs direct reuse at remote50 `14.05M/74.69 MiB` vs `14.38M/74.81 MiB`, and remote90 `10.73M/77.56 MiB` vs `10.66M/77.45 MiB`. Remote50 observe showed `direct_claim_success=3767`, Toy/MidPage split `77/3690`, returned backpressure/uncommitted zero, route mutation while pending zero, and direct integrity failure zero. Remote90 diagnostic rows are too heavy for zero gates, but shape observe showed most high-remote DirectReuse claims are MidPage and transfer-overlap (`3154` claims, `2624` MidPage, `2626` transfer-overlap in the full diagnostic row) | Current lazy-storage p1 changes make DirectReuse worth keeping as a profile candidate, but it trades a remote50 win for a small remote90 loss. Do not add DirectReuse to the high-remote profile/default yet; next behavior should prefer source-boundary/order policy over broader eager frontcache-miss DirectReuse |
| HZ6 Ubuntu `OwnerInboxSourceGateRecheck-L1` | `GO(research)/NO-GO(profile)` | Added owner-inbox tax-runner variants for `p1_external_source_gate` and `p1_external_source_gate_observe`, composing p1 external with `HZ6_REMOTE_PENDING_DIRECT_SOURCE_DEMAND_GATE_L1=1`. Production RUNS=3 measured p1 external vs frontcache-miss DirectReuse vs source-gate: remote50 `13.42M`, `13.63M`, `14.24M`; remote90 `10.53M`, `10.92M`, `3.99M`. Returned backpressure/uncommitted stayed zero in production-shaped runs | Source-boundary DirectReuse still starves the high-remote sink under the current maintenance ordering. Keep it as a research/phase control only; the next useful behavior should preserve owner-local maintenance on the high-remote path instead of moving all inline work to the source boundary |
| HZ6 Ubuntu `DirectReuseAsProfile-L1` | `GO(profile)/HOLD(default)` | Added explicit alias `hz6-high-remote-owner-inbox-direct-reuse-target` and builder `build_hz6_preload_high_remote_owner_inbox_direct_reuse_target.sh`, wiring it through the profile registry, HZ6 alias builder, and `bench_common.sh`. Registry check passed, the target DSO built, alias resolution returned the new DSO, and focused profile-frontier R1 smoke ran the existing high-remote owner-inbox profile against the DirectReuse profile with the DirectReuse alias ahead on all four focused rows | Keep this as an explicit profile/control only. It makes the DirectReuse candidate easy to run without changing selected/default or the high-remote owner-inbox profile; promotion still needs stronger remote90 and cross-platform evidence |
| HZ6 Ubuntu `DirectReuseTransferOutcomeAudit-L1` | `GO(tooling)/DESIGN checkpoint` | Extended the owner-inbox tax runner to extract existing transfer/source/frontcache counters plus DirectReuse transfer-overlap counters. Diagnostic RUNS=3 on `p1_external_direct_reuse_observe` showed remote50 median `direct_claim_success=3461`, `claim_success_transfer_nonempty=6`, `source_alloc=711`, `transfer_pop=2546`; remote90_short median `direct_claim_success=591`, `claim_success_transfer_nonempty=591`, `source_alloc=28628`, `transfer_pop=51559`. Remote90_short diagnostics still produce `returned_uncommitted`, so they remain shape-only | The transfer-overlap signal is strongly remote-ratio dependent: remote50 DirectReuse mostly does not steal transfer inventory, while remote90_short does. Next behavior, if any, should be a high-remote/transfer-aware guard or profile split, not a blanket DirectReuse skip |
| HZ6 Ubuntu `DirectReuseProfileReproBatch2-L1` | `HOLD(profile promotion)/NO-GO(next behavior)` | Re-ran production RUNS=10 for `p1_external` vs `p1_external_direct_reuse` after the profile alias and transfer-outcome audit. Batch 2 measured remote50 `14.18M/74.75 MiB` vs `14.05M/74.88 MiB`, and remote90 `10.67M/77.36 MiB` vs `10.48M/77.48 MiB`; returned backpressure/uncommitted and route mutation while pending stayed zero. Production DirectReuse counters are compiled out, as intended | The earlier DirectReuse remote50 win did not reproduce, and direct reuse lost both measured rows in this batch. Keep the explicit profile/control for future phase-shaped tests, but do not add a new transfer-aware skip or selected/profile promotion before a stronger paired-batch signal exists |
| HZ6 Ubuntu `PairedGateDistributionSummary-L1` | `GO(tooling)` | Updated the owner-inbox paired gate and tax attribution runners to summarize ops/s and peak RSS as min/p25/median/p75/max instead of median-only while retaining per-run TSVs and counter medians. `bash -n`, `git diff --check`, a production tax RUNS=1 remote50 p1 external smoke, and a paired-gate RUNS=1 p1 owner-inbox smoke passed | Use these summaries for the next paired RUNS=10 batches so variance is visible without reprocessing raw logs. This is observation-only and does not change allocator behavior |
| HZ6 Ubuntu `PairedGateR10Recheck-L1` | `HOLD(profile/default)/DESIGN checkpoint` | Re-ran the distribution paired gate with production RUNS=10. p0 selected-off beat p1 owner-inbox on every median row: local0 `16.74M/67.25 MiB` vs `16.25M/67.38 MiB`, remote50 `14.85M/69.50 MiB` vs `13.88M/74.81 MiB`, remote90 `11.00M/72.21 MiB` vs `10.67M/77.36 MiB`, and cross128_r90 `8.11M/68.81 MiB` vs `5.25M/74.56 MiB` | This removes the current performance case for promoting p1 owner-inbox as default or high-remote profile on this Ubuntu batch. Stop for design before adding more owner-inbox consumer policy; first reconcile why earlier p0-off high-remote cliffs disappeared in the current selected-off shape |
| HZ6 Ubuntu `OriginTransferCliffReconcile-L1` | `GO(evidence)/HOLD(owner-inbox)` | Added `p0_no_origin_transfer` to the owner-inbox tax runner. Production remote90 RUNS=3 measured current p0 selected-off at `11.24M/72.00 MiB` versus origin-transfer-off at `7.42M/103.05 MiB`; origin-transfer-off also raised `transfer_pop` from `89087` to `160903` and source allocation from `62864` to `67231` | The high-remote cliff is now mostly covered by the selected origin-transfer backpressure path, not owner-inbox. Keep owner-inbox p1 on hold and do not stack more owner-inbox policy until the origin-transfer selected path is treated as the current baseline |
| HZ6 Ubuntu `OriginTransferOutcomeCounters-L1` | `GO(tooling)/DESIGN checkpoint` | Added origin-transfer success/fail/stride/validation/full and full-occupancy counters to the owner-inbox tax runner. Production p0 remote50/remote90 RUNS=3 stayed healthy but detailed counters are diagnostic-only. Diagnostic shape showed remote50 `origin_success=7635`, `origin_full=28645`, avg full transfer occupancy `256`, max same-class `198`; remote90_short showed `origin_success=7184`, `origin_full=137`, avg full transfer occupancy `256`, max same-class `127` | Origin-transfer is the current selected high-remote baseline, but remote50 still sees a large full tail under diagnostic attribution. Stop for design before adding another sink; next behavior should target the remaining full tail without reintroducing owner-inbox runtime/RSS tax |
| HZ6 Ubuntu `OriginTransferFullTailDesign-L1` | `GO(design)/HOLD(behavior)` | Reviewed the selected origin-transfer and owner-local transfer reuse boundaries. Existing no-go evidence rules out remote-thread drain, generic overflow, owner-inbox reintroduction, larger transfer capacity, and broad class sharding as immediate next steps. The transfer consumer already tries same-class transfer reuse, so the next useful box is an observe-only `OriginTransferFullTailDemandAudit-L1` around source/prefill commits and transfer backlog | Do not implement another sink yet. First measure whether source allocation commits still happen while same-class transfer inventory exists, whether global-full is mostly same-class or cross-class pressure, and whether owner-local demand lags producer bursts |
| HZ6 Ubuntu `OriginTransferFullTailDemandAudit-L1` | `GO(tooling)/DESIGN checkpoint` | Added diagnostic-only counters for source/prefill commits with same-class or any transfer inventory, plus transfer-pop loop attempt/empty/invalid/hit attribution. Diagnostic p0 smoke showed numeric counters instead of `NA`; source/prefill commits did not overlap same-class transfer inventory in this sample, while pop loops split into valid hits and empty loops (`remote90_short`: `attempt=95891`, `empty=43324`, `hit=52567`) | The remaining origin-transfer full tail is not explained by missing same-class transfer backlog at source/prefill commit in this smoke. Keep this as observation tooling and stop for design before adding another sink or drain behavior |
| HZ6 Ubuntu `OriginTransferFullTailAttributionV2-L1` | `GO(tooling)/DESIGN checkpoint` | Added diagnostic buckets for origin-full same-class occupancy and transfer-pop empty-with-any-transfer. Diagnostic p0 smoke showed remote50 origin-full buckets spread across zero/low/mid/high same-class occupancy (`63/6041/1064/15793/6208`), while remote90_short pop-empty was mostly cross-class inventory (`empty=28670`, `empty_with_any=28309`, max transfer count `255`) | The tail is not a single same-class-capacity issue. The next design should treat class mismatch and producer/consumer phase timing explicitly; do not use broad capacity, drain, owner-inbox, or overflow as the immediate answer |
| HZ6 Ubuntu `OriginTransferPhaseAgeAudit-L1` | `GO(tooling)/DESIGN checkpoint` | Added diagnostic-only transfer demand epochs, transfer object publish-kind stamps, destination-vs-origin-fallback occupancy aggregates, and pop/full/empty phase-age buckets. Diagnostic p0 smoke showed origin publish attribution matching origin-transfer success (`remote50 origin_success=7810 origin_fallback_publish=7810`, `remote90_short 5048/5048`) with stamp/kind/generation zero gates clean; production p0 smoke compiled the audit out with all `origin_phase_audit_*` counters zero | Use this audit to classify the remaining origin-transfer tail as stale cross-class inventory, fresh phase lag, or demanded-not-consumed inventory before adding behavior. Keep A/victim, B/maintenance, and C/admission-gate on hold until the phase-age data justifies one narrow box |
| HZ6 Ubuntu `OriginTransferResidentSplitAudit-L1` | `GO(tooling)/DESIGN checkpoint` | Added requested-vs-resident transfer occupancy splits by destination/origin-fallback and cross-resident demand age. Diagnostic RUNS=3 p0 showed remote50 origin-full mixed same/cross origin-fallback pressure (`same origin=4.43M`, `cross origin=2.94M`) with fresh/recent/stale all present; remote90_short pop-empty had almost no same-class resident inventory (`same origin=2`) but huge cross-class resident inventory (`dest=3.64M`, `origin=0.57M`) split recent/stale (`2.29M/1.92M`) | Do not proceed to aged victim/demotion or admission rejection. The remaining selected transfer tail is a class-placement/phase-lag problem with recent cross-class inventory, so the next behavior design should be resident-class-aware placement or partitioning with a guaranteed sink |
| HZ6 Ubuntu `TransferClassPresenceGate-L1` | `GO(high-remote candidate)/HOLD(default)` | Added an opt-in per-transfer-cache committed class count so class-specific pop can skip the dense scan when requested-class inventory is absent. Production RUNS=3 measured p0 vs presence gate: remote50 `14.91M` vs `14.79M`, remote90 `3.75M` vs `10.81M`, remote90_short `4.58M` vs `8.54M`; local0 guard measured `16.74M` vs `16.27M`. Diagnostic invalid/underflow/over-capacity/placeholder/double inc/dec counters stayed zero, but lockless snapshot counters and false-zero shadow were nonzero | Keep as a strong high-remote candidate only. Do not promote to selected/default until the local0 tax is below budget and the false-zero/snapshot mismatch story is either removed or explicitly accepted as diagnostic-only with stronger shadow evidence |
| HZ6 Ubuntu `TransferClassPresenceProductionShape-L1` | `GO(high-remote candidate)/HOLD(default)` | Added `HZ6_TRANSFER_CLASS_PRESENCE_OBSERVE_L1` so production presence builds keep only `class_count[]`, compile out gate check/hit/miss and shadow RMWs, and return before class-count load on empty caches. RUNS=10 x 2 tax batches showed repeatable remote90 gains (`3.61M -> 10.53M`, `3.62M -> 10.55M`) and remote90_short gains (`4.54M -> 8.89M`, `4.74M -> 8.91M`). local0/remote50 are close but mixed, and paired cross128_r90 regressed (`8.73M -> 4.76M`) | Keep as the current high-remote candidate. Default remains HOLD; next behavior should test high-occupancy arming or profile-only packaging before changing selected/default |
| HZ6 Ubuntu `TransferClassPresenceProductionShapeR10Refresh-L1` | `GO(profile evidence)/HOLD(default)` | Re-ran production RUNS=10 for selected/off versus `p0_transfer_class_presence_min192` after the shape cleanup. Raw `hakozuna-hz6/private/raw-results/linux/hz6_presence_shape_promotion_20260620_155717`: local0 `16.73M -> 15.23M`, remote50 `14.76M -> 12.96M`, remote90 `4.04M -> 10.59M`, remote90_short `4.82M -> 8.83M`, cross128_r90 `6.65M -> 1.44M` | This confirms the profile split: min192 class presence is a strong remote90/high-remote profile, but remains NO-GO for selected/default while local0, remote50, and cross128_r90 regress in the same production batch |
| HZ6 Ubuntu `HighOccupancyPresenceGate-L1` | `GO(high-remote candidate)/HOLD(default)` | Added `HZ6_TRANSFER_CLASS_PRESENCE_MIN_TOTAL` and tested `p0_transfer_class_presence_min192`, which skips the class-count hint below 192 cached objects. Tax RUNS=10 showed p0 vs always-on vs min192: local0 `16.71M/16.14M/16.22M`, remote50 `14.82M/15.14M/14.86M`, remote90 `3.78M/10.51M/10.71M`, remote90_short `4.60M/8.76M/8.91M`. Paired RUNS=10 showed cross128_r90 partially recovered vs always-on (`3.50M -> 6.07M`) but still below p0 (`10.73M`) | Use min192 as the preferred high-remote presence candidate. Default remains HOLD because cross128_r90 still regresses; next step is a design checkpoint for profile packaging versus cross128-specific investigation |
| HZ6 Ubuntu `HighRemoteTransferPresenceProfile-L1` | `GO(profile)/HOLD(default)` | Added explicit alias `hz6-high-remote-transfer-presence-target` and builder `build_hz6_preload_high_remote_transfer_presence_target.sh`, wiring it through the shared profile builder, HZ6 alias autobuild layer, Ubuntu profile registry, and `bench_common.sh`. Registry check passed, the target DSO built, alias resolution returned the new DSO, and a direct LD_PRELOAD remote90_short smoke completed with fallback/overflow zero at `8.53M ops/s` | Keep min192 transfer class-presence as an explicit high-remote profile only. Selected/default remains unchanged until the cross128_r90 regression is explained or bounded |
| HZ6 Ubuntu `RemoteProfileFrontierRunner-L1` | `GO(tooling/profile evidence)` | Added `run_hz6_preload_remote_profile_frontier.sh`, a production-shaped MT remote runner for real profile aliases instead of internal tax variants. Smoke raw `hakozuna-hz6/private/raw-results/linux/hz6_remote_profile_frontier_smoke_20260620_160800` passed. R3 raw `hakozuna-hz6/private/raw-results/linux/hz6_remote_profile_frontier_20260620_160829` measured medians: selected remote50/remote90/cross128 `11.99M/4.05M/8.84M`; transfer_presence `14.60M/10.91M/8.61M`; owner_inbox `13.85M/11.05M/2.93M`; direct_reuse `13.79M/11.14M/5.20M`; small_class_shard `14.32M/4.07M/10.46M` | Use this runner for profile promotion checks. Current read: `transfer_presence` is the best high-remote general profile in this batch, while `small_class_shard` is the cross128 specialist. Do not change selected/default from this R3 alone |
| HZ6 Ubuntu `HighRemoteTransferPresenceProfileR10Frontier-L1` | `GO(profile)/HOLD(default)` | Re-ran the real-profile frontier with production RUNS=10 for selected versus `transfer_presence`. Raw `hakozuna-hz6/private/raw-results/linux/hz6_remote_profile_frontier_r10_20260620_161529`: local0 `16.87M -> 16.53M`, remote50 `14.74M -> 15.09M`, remote90 `3.84M -> 10.94M`, cross128_r90 `10.02M -> 4.51M`; fallback/overflow remained zero in the runner | Proceed with `transfer_presence` as the A/high-remote profile path. Do not promote it to selected/default because cross128 still regresses sharply; keep `small_class_shard` or a future cross128-specific box separate |
| HZ6 Ubuntu `SmallClassShardR10Frontier-L1` | `GO(cross128 specialist)/NO-GO(high-remote mix)` | Re-ran the real-profile frontier with production RUNS=10 for selected versus `small_class_shard`. Raw `hakozuna-hz6/private/raw-results/linux/hz6_small_class_frontier_r10_20260620_161816`: local0 `16.58M -> 16.17M`, remote50 `15.08M -> 15.17M`, remote90 `4.00M -> 4.09M`, cross128_r90 `7.04M -> 8.82M`; fallback/overflow remained zero in the runner | Keep `small_class_shard` as a separate cross128/remote50 specialist profile. Do not mix it into `transfer_presence`: it does not address the main remote90 cliff, while the previous combo row lost the transfer-presence remote50/cross128 balance |
| HZ6 Ubuntu `RemoteAllocatorCompareR10-L1` | `GO(evidence)/DESIGN checkpoint` | Added `run_hz6_remote_allocator_compare.sh` and ran MT remote RUNS=10 across system/HZ3/HZ4/HZ6 profiles/mimalloc/tcmalloc. Raw `hakozuna-hz6/private/raw-results/linux/hz6_remote_allocator_compare_r10_20260620_162815`, external continuation `hz6_remote_allocator_compare_ext_r10_20260620_163151`, and small-class cross128 recheck `hz6_small_class_cross128_recheck_20260620_163347`: best HZ6 remote90 was `transfer_presence 10.57M`, ahead of system/HZ3/mimalloc/tcmalloc but behind HZ4 `30.95M`; remote50 was dominated by tcmalloc `58.95M`; cross128 was dominated by HZ4/tcmalloc/HZ3 (`209.45M/184.71M/172.37M`) while best HZ6 recheck was `small_class_shard 12.19M` | HZ6 is improved but not the MT remote frontier. Stop profile-default tuning here; next performance design should study HZ4's remote90/cross128 shape or isolate HZ6 cross128 overhead. Treat `transfer_presence` as a high-remote HZ6 profile, not a default |
| HZ6 Ubuntu `Cross128RouteCostAudit-L1` | `GO(evidence)/DESIGN checkpoint` | Ran diagnostic cross128 R3 for p0 selected-off, transfer presence min192, and presence+small-class. Raw `hakozuna-hz6/private/raw-results/linux/hz6_cross128_route_cost_audit_20260620_163848`: p0 showed `returned_backpressure=125512`, `transfer_pop=514642`, `transfer_reserve_full=760803`, `origin_transfer_success=510635`, `origin_transfer_full=125512`; small-class reduced `transfer_pop` to `375324`, `transfer_reserve_full` to `624397`, and class-presence empty scan total to `33.06M`, but returned backpressure stayed about `125K` | The cross128 weakness is primarily tiny-object remote transfer pressure and transfer-cache scan/full cost, not source allocation. Next behavior should be a Toy/cross128 transfer-cache or same-size remote-free fast path, boxed away from MidPage/high-remote defaults |
| HZ6 Ubuntu `TransferPresenceSmallClassCombo-L1` | `NO-GO(profile)` | Added tax-runner variant `p0_transfer_presence_small_class`, combining `HZ6_TRANSFER_CLASS_PRESENCE_GATE_L1=1`, `HZ6_TRANSFER_CLASS_PRESENCE_MIN_TOTAL=192`, and `HZ6_PROFILE_TRANSFER_SHARD_CLASS_MAX_ID=3`. Production R3 raw `hakozuna-hz6/private/raw-results/linux/hz6_presence_small_class_combo_20260620_161211` measured p0/min192/combo medians: local0 `16.23M/16.47M/16.40M`, remote50 `14.98M/15.29M/12.57M`, remote90 `3.88M/11.13M/10.87M`, cross128_r90 `6.79M/2.97M/3.19M` | Do not combine transfer presence and small-class sharding. The combo preserves most remote90 benefit but loses the remote50 win and does not recover cross128; keep `transfer_presence` and `small_class_shard` as separate profiles |
| HZ6 Ubuntu `TransferPresenceThresholdAudit-L1` | `GO(tooling)/DESIGN checkpoint` | Added diagnostic-only `transfer_class_presence_below_min_check/hit/miss` counters and let the owner-inbox tax runner measure `cross128_r90`. Diagnostic R1 showed min192 had `presence_gate_check=0`, `below_min_check=719112`, `below_min_hit=370702`, `below_min_miss=348410`, and `false_zero_shadow=0` while still trailing p0 (`0.44M` vs `0.54M` diagnostic ops/s) | The cross128 regression is not caused by over-aggressive presence skip or false-zero; the hint never fires on this row. The remaining suspect is class-count atomic maintenance cost below the min threshold. Stop for design before attempting armed/lazy class-count maintenance |
| HZ6 Ubuntu `TransferPresenceUpdateCostAudit-L1` | `GO(tooling)/DESIGN checkpoint` | Added diagnostic-only `transfer_class_presence_update_below_min_increment/decrement/commit` counters. Diagnostic cross128 R1 showed min192 still had `presence_gate_check=0`, but `below_min_check=1270083`, `update_below_min_increment=661631`, `update_below_min_decrement=659031`, and `update_below_min_commit=661631` | Raising the min threshold alone cannot remove the cross128 tax because update-side class_count maintenance still runs below the threshold. Next behavior needs an armed/lazy class-count design that rebuilds counts on threshold crossing; do not skip below-threshold increments blindly |
| HZ6 Ubuntu `TransferPresenceArmedMaintenance-L1` | `GO(research)/NO-GO(profile)` | Added default-off `HZ6_TRANSFER_CLASS_PRESENCE_ARMED_L1=1` and tax-runner variant `p0_transfer_class_presence_min192_armed`. Diagnostic R1 showed below-threshold update counters drop to zero with `false_zero_shadow=0`; production R3 versus selected/off measured cross128_r90 `0.40M` vs `0.39M` and remote90_short `2.66M` vs `2.50M` | Keep armed maintenance as a research switch only. It removes the intended atomic update tax, but does not beat selected/off and should not be added to the high-remote transfer-presence profile |
| HZ6 Ubuntu `TransferPresencePopScanAudit-L1` | `GO(tooling)/HOLD(behavior)` | Added diagnostic-only pop scan distance counters for transfer cache hit and empty paths, then corrected `*_scan_max` aggregation to use max rather than sum. Diagnostic R1 showed large empty-scan totals while false-zero stayed zero: min192 cross128_r90 `empty_total=19810091`, remote90_short `6042559`; armed cross128_r90 `32570877`, remote90_short `9043920` | The remaining presence/profile question is now observable as dense scan shape when the hint does not fire. Keep this as tooling only and stop for design before changing cache layout, scan order, or profile packaging |
| HZ6 Ubuntu `TransferBelowMinPopScanLimit-L1` | `NO-GO(default/profile)` | Added default-off `HZ6_TRANSFER_BELOW_MIN_POP_SCAN_LIMIT=64` and tax-runner variant `p0_transfer_class_presence_min192_scan64`. Diagnostic R1 reduced empty scan work (`cross128_r90 26.7M -> 12.1M`, `remote90_short 6.1M -> 5.7M`), but production R3 regressed both medians: cross128_r90 `3.43M -> 2.53M`, remote90_short `8.89M -> 8.66M` | Do not bound below-min pop scans for the high-remote profile. The scan work reduction appears to trade against finding older same-class transfer entries, so keep this as an opt-in research switch only |
| HZ6 Ubuntu `TransferClassNegativeMemo-L1` | `NO-GO(correctness)` | Tested a local default-off negative absence memo that skipped below-threshold pop scans after a full dense miss, invalidated by a publish epoch. Diagnostic shadow caught false-zero cases (`cross128_r90 false_zero_shadow=11917`), so the implementation was removed before commit | Do not memoize class absence without a stronger synchronization boundary or a real per-class index. The next viable direction is an exact per-class index/list or profile packaging, not a lockless negative cache |
| HZ6 Ubuntu `TransferClassIndex-L1` | `NO-GO(correctness)` | Tested a local default-off sidecar per-class linked index over the shared dense transfer slot pool. Diagnostic smoke showed the side index/counts diverging (`cross128_r90 over_capacity=187909`, `scan_mismatch=19`; `remote90_short scan_mismatch=742`), so the implementation was removed before commit | Do not add sidecar class links to the current unsynchronized transfer cache. A safe O(1) class pop needs a real transfer-cache synchronization boundary or a replacement class-indexed storage design |
| HZ6 Ubuntu `TransferCacheLock-L1` | `GO(sync prerequisite)/HOLD(default, profile)` | Added default-off `HZ6_TRANSFER_CACHE_LOCK_L1` and tax-runner variant `p0_transfer_class_presence_min192_lock`, serializing transfer push/pop and reservation commit/cancel. Diagnostic R1 remote90_short cleaned the live presence divergence (`underflow 1 -> 0`, `scan_mismatch 5 -> 0`, `false_zero 0 -> 0`). Production RUNS=10 versus min192 measured local0 `16.64M -> 16.49M`, remote50 `14.98M -> 14.66M`, cross128_r90 `3.68M -> 8.18M`, remote90_short `8.88M -> 9.26M` | Keep as a research switch and synchronization prerequisite for future class-indexed transfer storage. Do not promote directly because remote50 still regresses and the lock is a boundary tool, not yet the final transfer-cache layout |
| HZ6 Ubuntu `TransferClassIndexLocked-L1` | `NO-GO(performance)` | Retested a dense-slot side index under the transfer-cache lock. Diagnostic R1 kept `underflow/scan_mismatch/false_zero` at zero and removed pop scan totals, but production R3 regressed versus lock-only: local0 `16.75M -> 16.43M`, remote50 `15.02M -> 14.39M`, cross128_r90 `13.29M -> 3.27M`, remote90_short `8.60M -> 8.86M`; implementation was removed before commit | Do not layer a linked side index onto the dense shared transfer array. The lock fixes coherence, but the side-index maintenance/traversal is too expensive; any future class-indexed attempt should replace the storage layout rather than patching this one |
| HZ6 Ubuntu `TransferCacheLockProfileCheck-L1` | `NO-GO(profile change)` | Checked whether `HZ6_TRANSFER_CACHE_LOCK_L1` should be mixed into the high-remote transfer-presence profile. Production remote90 R3 measured p0 selected-off `3.61M`, min192 `10.77M`, and min192+lock `10.84M` | Do not add the transfer-cache lock to `hz6-high-remote-transfer-presence-target`. It does not materially improve the main remote90 row and earlier guards showed remote50 tax |
| HZ6 Ubuntu `TransferPresenceArmedLockCombo-L1` | `NO-GO(profile)` | Added tax-runner variant `p0_transfer_class_presence_min192_armed_lock` to test the existing armed presence maintenance together with the transfer-cache lock. Production R3 raw `hakozuna-hz6/private/raw-results/linux/hz6_presence_armed_lock_20260620_160237`: min192/lock/armed-lock medians were local0 `16.25M/16.27M/16.02M`, remote50 `13.18M/14.43M/13.97M`, remote90_short `8.80M/9.55M/8.37M`, cross128_r90 `4.88M/5.09M/4.08M`. Main remote90 R3 raw `hakozuna-hz6/private/raw-results/linux/hz6_presence_armed_lock_remote90_20260620_160324` measured lock `10.62M` vs armed-lock `10.89M` | Keep the runner variant for reproducibility, but do not promote the combo. The small main remote90 gain does not offset remote90_short, remote50, and cross128 weakness; armed maintenance remains research-only |
| HZ6 Ubuntu `TransferClassSlotPool-L1` | `NO-GO(performance)` | Tested a local replacement transfer layout under the transfer-cache lock: free-slot pool plus per-class heads, with O(1) class pop. Diagnostic counters stayed clean and pop scan work was removed, but production R3 regressed cross128_r90 `15.06M -> 3.70M` while remote90_short improved `8.41M -> 9.02M`; implementation was removed before commit | Do not replace the dense transfer array with this simple slot-pool layout. It helps the narrow short row but breaks cross128; keep high-remote profile at min192 and treat further transfer-layout work as a new design problem |
| HZ6 Ubuntu `PendingDrainBudgetProbe-L1` | `NO-GO(cross128)` | Added owner-inbox tax variants for pending drain budget 2 and 4, with and without split maintenance. Production R3 raw `hakozuna-hz6/private/raw-results/linux/hz6_cross128_pending_budget_prod_r3_20260620_175718`: plain p1 cross128 `5.01M`, budget2 `1.08M`, budget4 `1.45M`; split cross128 `4.11M`, split+budget2 `3.27M`, split+budget4 `5.82M` with weak p25 `2.29M`. R10 follow-up raw `hakozuna-hz6/private/raw-results/linux/hz6_cross128_pending_budget_r10_20260620_180145` measured p1 `4.27M`, split `16.68M`, split+budget4 `2.17M` | Do not tune cross128 by increasing pending drain budget. Split maintenance has a real cross128 signal in this batch, but the useful shape is budget-1 work placement rather than more drain volume or DirectReuse stacking |
| HZ6 Ubuntu `ToyClass2FrontMaintenanceGate-L1` | `GO(profile candidate)/HOLD(default)` | Added a small pending-maintenance policy helper and tax-runner variant `p1_external_toy2_split_maintenance`, limiting split maintenance to Toy class 2 with budget 1 and DirectReuse off. R3 guard raw `hakozuna-hz6/private/raw-results/linux/hz6_toy2_split_prod_r3_20260620_180635`: p1/toy2 medians were remote50 `13.63M/13.54M`, remote90 `10.86M/11.47M`, cross128 `7.35M/7.86M`. R10 guard raw `hakozuna-hz6/private/raw-results/linux/hz6_toy2_split_guard_r10_20260620_181018`: p1/toy2 medians were remote50 `13.52M/13.52M`, remote90 `10.95M/11.06M`, cross128 `5.53M/11.55M` | Keep as a cross128 specialist profile candidate. It more than doubles cross128 versus p1 while keeping remote50 flat and remote90 slightly higher in the paired R10 guard. Default remains HOLD because this is a narrow Toy class 2 policy and broad selected behavior still needs cross-platform proof |
| HZ6 Ubuntu `Cross128Toy2SplitProfile-L1` | `GO(profile candidate)/HOLD(default)` | Added explicit alias `hz6-cross128-toy2-split-target`, builder `build_hz6_preload_cross128_toy2_split_target.sh`, registry/resolver wiring, and `toy2_split` support in the remote profile frontier. Registry check passed, alias resolution found the DSO. Profile R10 raw `hakozuna-hz6/private/raw-results/linux/hz6_toy2_profile_frontier_r10_20260620_181753`: selected `remote50/remote90/cross128=14.34M/3.83M/7.95M`, transfer_presence `14.25M/10.80M/3.92M`, small_class_shard `15.59M/3.93M/8.37M`, toy2_split `14.46M/11.17M/8.63M` | Keep as the best balanced high-remote/cross128 HZ6 profile candidate in this frontier. It is not the remote50 winner and not a default candidate; next compare it against HZ3/HZ4/mimalloc/tcmalloc before paper-facing claims |
| HZ6 Ubuntu `RemoteAllocatorCompareToy2R10-L1` | `GO(profile candidate)/HOLD(default)` | Extended `run_hz6_remote_allocator_compare.sh` with `toy2_split` and ran RUNS=10 across HZ3/HZ4/HZ6 profiles/mimalloc/tcmalloc. Raw `hakozuna-hz6/private/raw-results/linux/hz6_remote_allocator_compare_toy2_r10_20260620_182215`: HZ6 selected `remote50/remote90/cross128=15.09M/3.87M/7.17M`, transfer_presence `13.75M/10.70M/2.97M`, small_class_shard `14.54M/4.04M/8.00M`, toy2_split `13.64M/11.24M/35.95M`; external/legacy leaders were tcmalloc remote50 `71.18M`, HZ4 remote90 `29.31M`, HZ4 cross128 `207.61M` and tcmalloc cross128 `185.87M` | Keep `toy2_split` as the best HZ6 high-remote plus cross128 profile candidate, but not selected/default. HZ6 is now clearly improved inside its profile family, yet the tiny cross-owner frontier remains far behind HZ4/tcmalloc/HZ3 |
| HZ6 Ubuntu `Cross128Toy2Observe-L1` | `GO(observation)/DESIGN checkpoint` | Ran a diagnostic R3 for `p1_external` versus `p1_external_toy2_split_maintenance` and a production-shaped `perf stat` single run for HZ4, toy2_split, and tcmalloc. Raw `hakozuna-hz6/private/raw-results/linux/hz6_toy2_split_observe_r3_20260620_182932` and `hz6_cross128_perf_stat_20260620_183301`: Toy2 split increased frontcache hits and pending drain but did not reduce source allocation; perf showed toy2_split `20.83M ops/s`, `4.02B` instructions, `32.22M` cache misses versus HZ4 `204.58M`, `0.41B`, `6.84M` and tcmalloc `174.90M`, `0.25B`, `7.20M` | The remaining tiny cross-owner gap is mainly instruction/work count in the HZ6 free/pending/route path, not source supply. Next box should be a small-free path cost audit before behavior changes |
| HZ6 Ubuntu `Cross128SmallFreePathCostAudit-L1` | `GO(tooling)/DESIGN checkpoint` | Added existing preload/allocator free-path counters to the owner-inbox tax runner and ran diagnostic R3 on `cross128_r90`. Raw `hakozuna-hz6/private/raw-results/linux/hz6_cross128_small_free_path_cost_r3_20260620_183547`: both variants had `free_calls=960676`; Toy active-map hits were only `55,675` baseline and `59,483` Toy2, while `free_route_lookup_after_maps` stayed around `0.90M`; MidPage active-map hit was zero for this Toy-only row | The next behavior should target tiny free dispatch shape: avoid guaranteed MidPage probe where Toy ownership is known, improve Toy active-map coverage for remote Toy class2, or consume resolver proof without repeated map/route work. Do not tune source allocation or pending drain volume from this evidence |
| HZ6 Ubuntu `SkipMidPageFreeMapProbe-L1` | `NO-GO(default/profile)` | Added default-off `HZ6_FREE_SKIP_MIDPAGE_ACTIVE_MAP_L1` and a tax-runner research variant, then built an official-profile shaped `toy2_split + skip_midmap` DSO. Raw `hakozuna-hz6/private/raw-results/linux/hz6_toy2_skip_midmap_official_r5_20260620_184844`: base vs skip medians were remote50 `13.39M -> 4.04M`, remote90 `11.25M -> 1.53M`, cross128 `6.76M -> 32.92M`; remote90 RSS exploded to about `2932 MiB` | Do not promote a blanket MidPage free-map skip. It confirms the cross128 direction, but the next behavior must be a selective Toy-only free dispatch hint that preserves MidPage fast paths for mixed-size rows |
| HZ6 Ubuntu `ToyOnlyFreeDispatchHint-L1` | `NO-GO(profile candidate)` | Dry-run R3 raw `hakozuna-hz6/private/raw-results/linux/hz6_toy_pagekind_dryrun_r3_20260620_185518` kept `free_page_kind_selector_wrong_toy_page_mid_hit=0`, but coverage was sparse. Default-off behavior R3 raw `hakozuna-hz6/private/raw-results/linux/hz6_toy_page_skip_midpage_r3_20260620_185728` regressed base/toy-page-skip medians: remote50 `13.10M -> 11.52M`, remote90 `11.50M -> 11.32M`, cross128 `13.28M -> 3.99M` | Keep the implementation default-off only for research. Page-kind Toy proof is safe in this short run but too sparse/expensive; next small-free work should target route/resolver work after active-map miss or Toy active-map coverage |
| HZ6 Ubuntu `PreloadForeignRouteBeforeMaps-L1` | `GO(profile candidate)/HOLD(default)` | Default-off R10 raw `hakozuna-hz6/private/raw-results/linux/hz6_foreign_route_before_maps_r10_20260620_190239`: base vs route-before-maps medians were remote50 `13.59M -> 14.36M`, remote90 `11.42M -> 11.34M`, cross128 `5.93M -> 15.67M`. TLS-armed R10 raw `hakozuna-hz6/private/raw-results/linux/hz6_foreign_route_before_maps_armed_tls_r10_20260620_191029`: local0 `16.09M -> 16.71M`, remote50 `14.11M -> 14.24M`, remote90 `11.27M -> 11.09M`, cross128 `11.77M -> 18.52M`. Local-valid dispatch follow-up R10 raw `hakozuna-hz6/private/raw-results/linux/hz6_route_before_maps_local_dispatch_r10_20260620_191859`: toy2_split base vs candidate medians were local0 `16.29M -> 16.51M`, remote50 `13.87M -> 14.10M`, remote90 `11.23M -> 11.46M`, cross128 `10.78M -> 28.59M` | Keep `hz6-cross128-toy2-route-before-maps-target` as an explicit cross128/high-remote profile candidate using TLS arming plus local-valid dispatch. It now improves the paired local0/remote50/remote90/cross128 rows, but selected/default remains HOLD pending broader guards and cross-platform confirmation |
| HZ6 Ubuntu `RemoteAllocatorCompareRouteBeforeMapsR10-L1` | `GO(evidence)/DESIGN checkpoint` | Added `route_before_maps` to `run_hz6_remote_allocator_compare.sh` and ran RUNS=10 against HZ3/HZ4/HZ6 toy2/mimalloc/tcmalloc. Raw `hakozuna-hz6/private/raw-results/linux/hz6_remote_allocator_compare_route_before_maps_r10_20260620_192235`: `route_before_maps` beat `toy2_split` on HZ6 remote90/cross128 (`11.21M -> 11.39M`, `7.65M -> 10.89M`), but external leaders remained far ahead: tcmalloc remote50 `67.44M`, HZ4 remote90 `30.91M`, HZ4/tcmalloc/HZ3 cross128 `209.01M/187.10M/175.65M`. Added `--interleave-runs` and p25/p75 summary to the compare runner; raw `hakozuna-hz6/private/raw-results/linux/hz6_route_before_maps_interleave_r5_20260620_195150` showed both HZ6 profiles in the same weak remote90 band (`toy2_split 4.07M`, `route_before_maps 4.10M`) while route-before-maps still improved cross128 (`4.57M -> 9.88M`) | Treat `route_before_maps` as the current best HZ6 explicit high-remote/cross128 profile, not selected/default. Use interleaved runner order for future narrow HZ6 profile A/B so batch-level remote90 variance is not mistaken for a single-flag effect |
| HZ6 Ubuntu `SmallObjectObservationRefresh-L1` | `GO(observation)/NO-GO(behavior)` | Added an observation-only checkpoint before further tiny-object optimization. Diagnostic R3 raw `hakozuna-hz6/private/raw-results/linux/hz6_small_object_observation_refresh_r3_20260620_195735` compared `p1_external_toy2_split_maintenance` against `p1_external_toy2_route_before_maps` on `cross128_r90`: route-before-maps raised diagnostic median ops/s `0.46M -> 0.74M` and cut route-after-map lookups `901,649 -> 116,268`. Follow-up production/interleaved R5 raw `hakozuna-hz6/private/raw-results/linux/hz6_route_before_maps_pair_prod_interleave_r5_20260620_213806` did not preserve the cross128 win: `toy2_split` vs `route_before_maps` medians were remote90 `4.04M -> 3.98M`, cross128 `36.81M -> 36.18M`, with route-before-maps cross128 p25 collapsing to `5.66M`. Phase-counter-only R10 raw `hz6_route_before_maps_cross128_phase_counters_r10_20260620_215725` split the low cross128 runs: slow runs had `alloc_fail == PROVEN_EXTERNAL fallback` plus descriptor/source exhaustion. Capacity follow-up raw `hz6_route_before_maps_capacity_split_r5_20260620_220520` showed `wide12288` removed exhaustion in phase-counter runs, while production raw `hz6_route_before_maps_wide_capacity_prod_r10_20260620_220558` still tailed with exhaustion. Extra-wide production raw `hz6_route_before_maps_xwide_capacity_prod_r5_20260620_220659` removed exhaustion and stabilized cross128 `30.36M..35.01M`, but peak RSS rose to about `195 MiB` and remote90 stayed around `4.01M` | Do not add another `PROVEN_EXTERNAL` fallback consumer, and do not promote broad static capacity. The next behavior box should target the descriptor/source exhaustion cliff with narrow Toy class2 reclamation or adaptive capacity |
| HZ6 Ubuntu `Toy2UnretainedSourceBlockAbort-L1` | `GO(profile research)/HOLD(default)` | Added a default-off Toy class2 construction-abort path for newly created source blocks whose prefill materialized zero slots. Raw R5 `hakozuna-hz6/private/raw-results/linux/hz6_toy2_construct_abort_r5_20260620_225549`: route-before-maps vs abort medians on `cross128_r90` were `11.13M -> 17.34M`, with `alloc_fail 4768 -> 520`, `descriptor_exhausted 10496 -> 1561`, and `source_block_exhausted 3319 -> 0`. `remote90` stayed flat (`11.20M -> 11.15M`) and abort did not fire there. Hard abort gates remained zero: ref nonzero, live descriptor, live route, pending reference, double release, release fail, and active-after-abort | The zero-ref source-block construction rollback gap is real and worth keeping as profile research. Do not increase source capacity from this evidence; remaining failures are descriptor pressure, so the next box should be Toy2 descriptor pressure audit before lazy descriptor segments |
| HZ6 Ubuntu `Toy2DescriptorPressureAudit-L1` | `GO(observation)` | Added behavior-free Toy class2 descriptor-failure snapshots at front/class-aware failure sites. Raw R5 `hakozuna-hz6/private/raw-results/linux/hz6_toy2_descriptor_pressure_audit_r5_20260620_231121` on `p1_external_toy2_route_before_maps_abort`: `cross128_r90` median had `alloc_fail=1421`, `descriptor_exhausted=4264`, `source_block_exhausted=0`, `toy2_descriptor_fail=2843`, with Toy2 `active_max=7748`, `local_free_max=64`, `transfer_free_max=137`, `remote_pending_max=0`, and source-block ref-zero/full/partial all zero. `remote90` was a weak batch but Toy2 descriptor audit stayed zero there | Remaining cross128 failures are true Toy2 active descriptor pressure, not source-block record pressure or partial-run reuse. Proceed to Toy2 lazy descriptor segment with route headroom and segment high-water gates before touching source capacity |
| HZ6 Ubuntu `Toy2AdaptiveDescriptorSegment-L1` | `GO(profile candidate)/HOLD(default)` | Added default-off Toy class2 lazy descriptor segments with route headroom and segment integrity gates. Fastpath R5 raw `hakozuna-hz6/private/raw-results/linux/hz6_toy2_adaptive_descriptor_fastpath_r5_20260620_232941`: abort vs adaptive medians were `remote90 11.15M -> 11.26M` and `cross128_r90 21.47M -> 33.23M`, with cross128 `alloc_fail 385 -> 0`, `toy2_descriptor_fail 771 -> 0`, adaptive segment high-water 3, and adaptive max/route/double/reuse/owner-mismatch gates zero. Interleaved R10 raw `hakozuna-hz6/private/raw-results/linux/hz6_toy2_adaptive_descriptor_interleave_r10_20260620_235034` showed the stable profile shape: `remote90 3.70M -> 11.09M`, `cross128_r90 35.84M -> 34.78M`, `local0 16.58M -> 16.58M`, `remote50 14.21M -> 14.22M`, with adaptive cross128 `alloc_fail=0`, `toy2_descriptor_fail=0`, and `segment_high_water=2` | This closes the post-abort Toy2 descriptor capacity cliff without increasing source-block capacity. Keep default-off until the profile target is explicitly folded in; `descriptor_exhausted` still records base-table misses before adaptive fallback succeeds, so gate on final `alloc_fail`/`toy2_descriptor_fail` and the stable interleaved tail |
| HZ6 Ubuntu `RouteBeforeMapsCross128TailStats-L1` | `GO(observation)/DESIGN checkpoint` | Diagnostic R10 raw `hakozuna-hz6/private/raw-results/linux/hz6_route_before_maps_cross128_tail_stats_r10_20260620_192907`: all runs kept `remote_free_returned_backpressure=0`, while low diagnostic ops correlated with early resolver fallback classified as `PROVEN_EXTERNAL` and the same later route lookup count. Fastest run had fallback/after-map lookup about `253K`; most slower runs had about `390K-400K`. Follow-up external-dispatch stats R3 raw `hakozuna-hz6/private/raw-results/linux/hz6_route_before_maps_external_dispatch_stats_r3_20260620_194008` confirmed `PROVEN_EXTERNAL` fallback could be consumed early with `free_route_real_free_unproven=0`. Production R10 raw `hakozuna-hz6/private/raw-results/linux/hz6_route_before_maps_external_dispatch_r10_20260620_194051` improved cross128 `20.65M -> 36.37M` but had weak remote90. A later off/on diagnostic and production verify (`hz6_route_before_maps_external_dispatch_diag_matrix_r3_20260620_194533`, `hz6_route_before_maps_external_dispatch_prod_verify_r5_20260620_194706`) showed external dispatch was not the sole remote90 cause: diagnostic remote90 had `external_dispatch=0`, and production remote90 was weak in both off/on builds | `PreloadRouteBeforeMapsExternalDispatch-L1` remains NO-GO for the route-before-maps profile and default-off research only. Do not include it in `hz6-cross128-toy2-route-before-maps-target`; next work should separate cross128 tail improvement from remote90 stability with paired same-batch profile checks |
| HZ6 Ubuntu `RemoteFreeBackpressureOriginDrain-L1` | `NO-GO(default)` | Opt-in smoke converted most origin full events (`origin_drain_retry_success=12160`, `origin_transfer_full=32`) with integrity gates clean, but RUNS=3 regressed to `remote50=13935320.47`, `remote90=1287581.08` | Keep as an opt-in research box only; draining transfer objects into the origin frontcache on the remote path is too expensive for the selected lane |
| HZ6 Ubuntu `PreloadMallocTransferRetry-L1` | `NO-GO(default)` | Opt-in smoke consumed transfer backlog (`preload_malloc_transfer_retry_hit=2690/3040`) and reduced backpressure with integrity gates clean, but RUNS=3 was weak at `remote50=14613277.36`, `remote90=1077952.16` | Keep as opt-in only; owner-local malloc transfer retry helps counters but still makes remote90 too slow |

## Mac Notes

- The current Mac pass was measured on an Apple Silicon M1 machine.
- Mac preload uses `DYLD_INSERT_LIBRARIES`, not Linux `LD_PRELOAD`.
- Linux `perf` is not the Mac primary path here; use the Mac observe lane and counters instead.
- `mimalloc` and `tcmalloc` staying strong on Mac is now treated as a code-shape question, not a Mac-only build-flags question.
- The current working tree now has a fresh `hz3` rerun that lines back up with the paper-era intuition: `hz3` is broad and strong on Mac too, so stale library paths and mismatched artifacts are now a more likely explanation for the old weak Mac read.
- Keep Mac-specific setup in [`docs/MAC_BUILD.md`](../MAC_BUILD.md), [`docs/MAC_BENCH_PREP.md`](../MAC_BENCH_PREP.md), and [`mac/README.md`](../../mac/README.md).

## Pointers

- [Benchmark README](./README.md)
- [Cross-platform conditions](./CROSS_PLATFORM_BENCH_CONDITIONS.md)
- [Mac benchmark results, 2026-03-19](./2026-03-19_MAC_BENCH_RESULTS.md)
