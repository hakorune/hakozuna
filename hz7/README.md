# HZ7 Family Index

This directory is the umbrella index for the HZ7 line.

The phased code trees now live here:

- `v1/` frozen cute baseline
- `v2/` tiny direct-API / remote-safe phase
- `v3/` performance-growth fork
- `v4/` remote-free safe research fork

The goal is to make the HZ7 family read as one phased line without losing the
history or benchmark trail that each phase already accumulated.

## Phase Map

- [v1](v1/README.md): frozen cute baseline
- [v2](v2/README.md): tiny direct-API / remote-safe phase
- [v3](v3/README.md): performance-growth fork
- [v4](v4/README.md): remote-free safe research fork

## Notes

- New docs and future phase work should prefer the `hz7/` umbrella paths.
